{
  "name": "biometric-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo para sistema biométrico con backend Python y frontend React",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "install:all": "npm install && cd apps/backend && uv sync && cd ../frontend && npm install"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "packageManager": "npm@10.0.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
