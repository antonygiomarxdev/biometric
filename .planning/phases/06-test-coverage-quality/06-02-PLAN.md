---
phase: 06-test-coverage-quality
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - "apps/backend/tests/core/compliance/test_strategy.py"
  - "apps/backend/tests/core/compliance/test_logger.py"
  - "apps/backend/tests/core/compliance/test_masking.py"
  - "apps/backend/tests/core/compliance/test_encryption.py"
autonomous: true
requirements: ["COV-01"]
must_haves:
  truths:
    - "Compliance core classes achieve >90% code coverage."
    - "PII scrubber, masking, and encryption logics are fully validated with edge cases."
  artifacts:
    - path: "apps/backend/tests/core/compliance/test_strategy.py"
      provides: "Tests for compliance strategies"
    - path: "apps/backend/tests/core/compliance/test_encryption.py"
      provides: "Tests for data encryption functions"
  key_links:
    - from: "apps/backend/tests/core/compliance/"
      to: "apps/backend/src/core/compliance/"
      via: "pytest imports"
---

<objective>
Write isolated unit tests for the Compliance Core module (`src/core/compliance/`).
Purpose: These functions govern the security, masking, logging, and encryption of PII data, making them critical for ISO 27037 compliance.
Output: Comprehensive test suites ensuring 100% coverage on core compliance logic.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ksante/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/backend/src/core/compliance/
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Test Strategy and Logger Modules</name>
  <files>apps/backend/tests/core/compliance/test_strategy.py, apps/backend/tests/core/compliance/test_logger.py</files>
  <behavior>
    - Test 1: Instantiating strategies behaves correctly depending on the provided compliance config.
    - Test 2: The logger correctly scrubs defined PII variables in various string formats.
  </behavior>
  <action>Create the tests directories and files if they don't exist. Implement strict unit tests using `pytest` for `strategy.py`, `factory.py`, and `logger.py`. Verify that the logger properly strips out sensitive forensic data patterns. Apply Clean Code principles and strict type hints in the test functions.</action>
  <verify>
    <automated>cd apps/backend &amp;&amp; PYTHONPATH=. pytest tests/core/compliance/test_strategy.py tests/core/compliance/test_logger.py -v</automated>
  </verify>
  <done>Strategy and Logger modules pass tests successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test Masking and Encryption Modules</name>
  <files>apps/backend/tests/core/compliance/test_masking.py, apps/backend/tests/core/compliance/test_encryption.py</files>
  <behavior>
    - Test 1: AI Data Tokenizer properly masks PII fields without data loss in recovery.
    - Test 2: Storage encryption successfully encrypts and decrypts with given keys, rejecting invalid formats.
  </behavior>
  <action>Write unit tests for `masking.py` and `encryption.py`. Ensure edge cases like null values, empty strings, and malformed inputs are covered. Assert that encryption functions never return plaintext. These are pure functions so mocks shouldn't be strictly necessary unless external libs are involved.</action>
  <verify>
    <automated>cd apps/backend &amp;&amp; PYTHONPATH=. pytest tests/core/compliance/test_masking.py tests/core/compliance/test_encryption.py -v</automated>
  </verify>
  <done>Masking and Encryption tests pass with high coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Compliance Module Coverage</name>
  <files>apps/backend/pyproject.toml</files>
  <action>Run a specific coverage report strictly over the `src/core/compliance/` module to assert the >90% goal is met for this slice. Address any small missing lines if reported.</action>
  <verify>
    <automated>cd apps/backend &amp;&amp; PYTHONPATH=. pytest tests/core/compliance/ --cov=src.core.compliance --cov-fail-under=90</automated>
  </verify>
  <done>Coverage for `src/core/compliance` explicitly surpasses 90%.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Testing Core Secrets | Mocked keys and secrets in test files must be dummy values only. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | `test_encryption.py` | mitigate | Hardcode dummy encryption keys in test setup; do not use `.env` prod secrets. |
</threat_model>

<verification>
Coverage metric for `src/core/compliance/` outputs `>90%` upon completion.
</verification>

<success_criteria>
`pytest tests/core/compliance/ --cov=src.core.compliance` passes above 90% without syntax or runtime errors.
</success_criteria>

<output>
Create `.planning/phases/05-test-coverage-quality/05-02-SUMMARY.md` when done
</output>
