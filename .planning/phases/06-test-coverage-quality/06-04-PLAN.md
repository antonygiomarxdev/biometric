---
phase: 06-test-coverage-quality
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - "apps/backend/tests/api/test_fingerprint_router.py"
  - "apps/backend/tests/api/test_auth_router.py"
  - "apps/backend/tests/api/test_audit_router.py"
  - "apps/backend/tests/services/test_fingerprint_service.py"
autonomous: true
requirements: ["COV-03"]
must_haves:
  truths:
    - "API endpoints and underlying services have >90% code coverage."
    - "FastAPI endpoints are tested using the TestClient with mocked dependencies (e.g., get_db)."
  artifacts:
    - path: "apps/backend/tests/api/test_fingerprint_router.py"
      provides: "HTTP integration tests for fingerprints"
    - path: "apps/backend/tests/services/test_fingerprint_service.py"
      provides: "Unit tests for the fingerprint service layer"
  key_links:
    - from: "apps/backend/tests/api/"
      to: "apps/backend/src/api/"
      via: "FastAPI TestClient"
---

<objective>
Write test suites for the Routers (`src/api/v1/`) and Services (`src/services/`) layers.
Purpose: Secure the entry points and business logic execution. We need >90% coverage for HTTP integrations while safely overriding database dependencies.
Output: High coverage tests utilizing `FastAPI.testclient`.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ksante/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/backend/src/api/
@apps/backend/src/services/
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Services Layer Coverage</name>
  <files>apps/backend/tests/services/test_fingerprint_service.py, apps/backend/tests/services/test_auth_service.py</files>
  <behavior>
    - Test 1: Auth service accurately signs and verifies tokens.
    - Test 2: Fingerprint service handles file uploads, coordinates storage and invokes processing.
  </behavior>
  <action>Create or update isolated unit tests for modules in `src/services/`. Mock the data repository, storage interfaces, and AI processing tasks. Ensure that exceptions raised by these boundaries are correctly handled by the services.</action>
  <verify>
    <automated>cd apps/backend &amp;&amp; PYTHONPATH=. pytest tests/services/ -v</automated>
  </verify>
  <done>Services layer passes tests successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Routers Integration Coverage</name>
  <files>apps/backend/tests/api/test_fingerprint_router.py, apps/backend/tests/api/test_auth_router.py</files>
  <behavior>
    - Test 1: POST /api/v1/fingerprints/ yields 200 OK or proper validation errors.
    - Test 2: Protected routes accurately reject unauthorized TestClient requests with 401.
  </behavior>
  <action>Write `TestClient` based integration tests for `src/api/routers/`. Use the `app.dependency_overrides` mechanism initialized in Phase 05 Plan 01 to override `get_db` and auth dependencies. Validate both successful responses and error schemas.</action>
  <verify>
    <automated>cd apps/backend &amp;&amp; PYTHONPATH=. pytest tests/api/ -v</automated>
  </verify>
  <done>FastAPI routes pass unit tests securely.</done>
</task>

<task type="auto">
  <name>Task 3: Verify API &amp; Services Coverage Goal</name>
  <files>apps/backend/pyproject.toml</files>
  <action>Run the coverage report specifically on `src.api` and `src.services` to ensure it hits the >90% mark. Iteratively fill any gaps exposed.</action>
  <verify>
    <automated>cd apps/backend &amp;&amp; PYTHONPATH=. pytest tests/api/ tests/services/ --cov=src.api --cov=src.services --cov-fail-under=90</automated>
  </verify>
  <done>Coverage metric for API and Services formally exceeds 90%.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client API | All API endpoints must appropriately validate parameters even during tests |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Spoofing | Auth Routes | mitigate | Ensure token validation tests explicitly verify failure modes (expired, forged, empty). |
</threat_model>

<verification>
Global coverage check `pytest --cov=src` runs cleanly to completion without throwing unhandled dependency injection errors.
</verification>

<success_criteria>
`pytest tests/api/ tests/services/ --cov=src.api --cov=src.services` passes >90% coverage successfully.
</success_criteria>

<output>
Create `.planning/phases/05-test-coverage-quality/05-04-SUMMARY.md` when done
</output>
