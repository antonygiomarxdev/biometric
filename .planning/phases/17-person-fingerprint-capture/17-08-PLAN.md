---
phase: 17-person-fingerprint-capture
plan: 08
type: execute
wave: 8
depends_on: ["17-04"]
files_modified:
  - apps/backend/src/api/routers/known_fingerprints.py
  - apps/backend/src/api/routers/latent_search.py
  - apps/backend/tests/integration/test_capture_e2e.py
autonomous: true
requirements:
  - e2e-verification
  - known-fingerprints-deprecation
user_setup: []

must_haves:
  truths:
    - /known-fingerprints/ is deprecated but works
    - E2E test runs the full 4-level flow
  artifacts:
    - test_capture_e2e.py
  key_links:
    - Proves the entire Phase 17 refactor works end-to-end
---

<objective>
Finalise Phase 17: deprecate the old flat enrollment endpoint, add
optional scope filters to search, and prove the full chain with an E2E test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/phase-17-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deprecate known_fingerprints and update search</name>
  <files>apps/backend/src/api/routers/known_fingerprints.py, apps/backend/src/api/routers/latent_search.py</files>
  <read_first>apps/backend/src/api/routers/known_fingerprints.py</read_first>
  <action>
1. Add `deprecated=True` to `@router.post("/")` in `known_fingerprints.py`.
2. Return a `Deprecation` header.
3. In `latent_search.py`, add optional `finger_position: int | None = None` and `top_k_per_finger: bool = False` to query params (implementation of these filters in the service is optional/best-effort for now, but API contract should support them).
  </action>
  <verify>PYTHONPATH=src python3 -m pytest tests/api/test_api_e2e.py</verify>
  <acceptance_criteria>
    - Endpoint marked deprecated
    - Search endpoint accepts new params
  </acceptance_criteria>
  <done>Endpoints finalised</done>
</task>

<task type="auto">
  <name>Task 2: Full E2E Capture test</name>
  <files>apps/backend/tests/integration/test_capture_e2e.py</files>
  <read_first>apps/backend/src/api/routers/captures.py</read_first>
  <action>
Create an integration test that:
1. Creates a person
2. Creates a fingerprint slot
3. Uploads an image (synthetic bytes)
4. Verifies models exist in Postgres
  </action>
  <verify>PYTHONPATH=src python3 -m pytest tests/integration/test_capture_e2e.py</verify>
  <acceptance_criteria>
    - E2E test passes
    - Covers the 4-level model insertion
  </acceptance_criteria>
  <done>E2E complete</done>
</task>

</tasks>
