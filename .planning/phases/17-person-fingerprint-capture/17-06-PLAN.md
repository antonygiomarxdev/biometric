---
phase: 17-person-fingerprint-capture
plan: 06
type: execute
wave: 6
depends_on: ["17-03"]
files_modified:
  - apps/backend/src/db/qdrant_chunk_repository.py
  - apps/backend/src/db/nebula_repository.py
  - apps/backend/tests/db/test_qdrant_chunk_repository.py
  - apps/backend/tests/db/test_nebula_repository.py
autonomous: true
requirements:
  - qdrant-payload-extension
  - nebula-tag-extension
user_setup: []

must_haves:
  truths:
    - Qdrant chunk payload includes capture_id and graph_id
    - NebulaGraph minutia tag includes capture_id and graph_id
    - Backward compatibility for existing payloads (missing fields default to None)
  artifacts:
    - qdrant_chunk_repository.py
    - nebula_repository.py
  key_links:
    - FingerprintEnrollmentService uses the new fields
---

<objective>
Update the vector store (Qdrant) and graph store (NebulaGraph) to
persist the new 4-level forensic schema relationships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/phase-17-SPEC.md
@apps/backend/src/db/qdrant_chunk_repository.py
@apps/backend/src/db/nebula_repository.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Qdrant chunk payload</name>
  <files>apps/backend/src/db/qdrant_chunk_repository.py, apps/backend/tests/db/test_qdrant_chunk_repository.py</files>
  <read_first>apps/backend/src/db/qdrant_chunk_repository.py</read_first>
  <action>
Update `bulk_insert_chunks` to accept optional `capture_id: str = None` and `graph_id: str = None`.
Add them to the payload.
Update tests to verify.
  </action>
  <verify>PYTHONPATH=src python3 -m pytest tests/db/test_qdrant_chunk_repository.py</verify>
  <acceptance_criteria>
    - capture_id and graph_id stored in payload
    - tests pass
  </acceptance_criteria>
  <done>Qdrant updated</done>
</task>

<task type="auto">
  <name>Task 2: Extend NebulaGraph minutia tag</name>
  <files>apps/backend/src/db/nebula_repository.py, apps/backend/tests/db/test_nebula_repository.py</files>
  <read_first>apps/backend/src/db/nebula_repository.py</read_first>
  <action>
Update `ensure_space()` to include `capture_id string` and `graph_id string` on the `minutia` tag.
Update `insert_graph` to accept and write these.
  </action>
  <verify>PYTHONPATH=src python3 -m pytest tests/db/test_nebula_repository.py</verify>
  <acceptance_criteria>
    - ensure_space includes new fields
    - tests pass
  </acceptance_criteria>
  <done>NebulaGraph updated</done>
</task>

</tasks>
