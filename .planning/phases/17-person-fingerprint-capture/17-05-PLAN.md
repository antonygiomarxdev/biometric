---
phase: 17-person-fingerprint-capture
plan: 05
type: execute
wave: 5
depends_on: ["17-03"]
files_modified:
  - apps/backend/src/processing/graph_extractor.py
  - apps/backend/src/services/fingerprint_service.py
  - apps/backend/tests/processing/test_graph_extractor.py
autonomous: true
requirements:
  - ridge-graph-extractor-components
user_setup: []

must_haves:
  truths:
    - RidgeGraphExtractor returns list[RidgeGraph] instead of RidgeGraph
    - Connected components of the skeleton are split into independent graphs
    - FingerprintService.process_image handles the list
  artifacts:
    - graph_extractor.py updated
    - test_graph_extractor.py updated
  key_links:
    - FingerprintEnrollmentService (Plan 03) can iterate over graphs directly
---

<objective>
Refactor RidgeGraphExtractor to return multiple disconnected components
as separate graphs.

Forensic latents often have disconnected ridges. Treating them as one
graph creates false edges across empty space. This refactor makes the
extractor return a `list[RidgeGraph]`, one per connected component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/phase-17-SPEC.md
@apps/backend/src/processing/graph_extractor.py
@apps/backend/src/core/types.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RidgeGraphExtractor returns list[RidgeGraph]</name>
  <files>apps/backend/src/processing/graph_extractor.py, apps/backend/tests/processing/test_graph_extractor.py, apps/backend/src/services/fingerprint_service.py</files>
  <read_first>apps/backend/src/processing/graph_extractor.py, apps/backend/src/core/types.py</read_first>
  <action>
Modify `RidgeGraphExtractor` in `apps/backend/src/processing/graph_extractor.py`.

1. Change `process()` to write a `list[RidgeGraph]` to `ctx.ridge_graphs` (you will need to add `ridge_graphs: list[RidgeGraph] | None = None` to `PipelineContext` in `src/core/interfaces.py`).
2. Use `nx.connected_components()` to split the graph built by `sknw.build_sknw()`.
3. For each connected component, build a separate `RidgeGraph`.
4. Update `FingerprintService.process_image` (and async) to attach all graphs to the final normalized output if needed, or leave them on context.
5. Update `test_graph_extractor.py` to expect a list.

**Note:** For this task, ensure backward compatibility for other tests. Since `NormalizedFingerprint` doesn't currently store graphs, the graphs just live on `ctx`.
  </action>
  <verify>PYTHONPATH=src python3 -m pytest tests/processing/test_graph_extractor.py</verify>
  <acceptance_criteria>
    - RidgeGraphExtractor produces a list
    - Tests pass
  </acceptance_criteria>
  <done>graph extractor returns list</done>
</task>

</tasks>
