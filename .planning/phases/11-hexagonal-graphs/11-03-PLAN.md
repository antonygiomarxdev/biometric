---
phase: "11-hexagonal-graphs"
plan: "03"
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - "apps/backend/src/services/topological_matching_service.py"
  - "apps/backend/tests/services/test_topological_matching_service.py"
autonomous: true
requirements: ["PHASE11-03"]

must_haves:
  truths:
    - "A latent fingerprint graph can be matched against known DB graphs using subgraph isomorphism."
    - "The service scores matches based on topological similarity."
  artifacts:
    - path: "apps/backend/src/services/topological_matching_service.py"
      provides: "TopologicalMatchingService"
  key_links:
    - from: "apps/backend/src/services/topological_matching_service.py"
      to: "apps/backend/src/db/repositories/topological_graph_repository.py"
      via: "dependency injection"
      pattern: "repository.*fetch"
---

<objective>
Implement the core Subgraph Isomorphism matching service.

Purpose: Perform 1-to-N matching by finding the latent fingerprint's subgraph within the known enrolled graphs using NetworkX.
Output: `TopologicalMatchingService`.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/backend/src/processing/graph_builder.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement TopologicalMatchingService</name>
  <files>apps/backend/src/services/topological_matching_service.py, apps/backend/tests/services/test_topological_matching_service.py</files>
  <behavior>
    - Test: `match_latent` takes a latent graph (JSON dict) and returns a list of candidate matches scored by isomorphic subgraph size.
    - Test: Correctly parses JSONB into `networkx.Graph` objects.
    - Test: If no subgraph isomorphism is found, score is 0.
  </behavior>
  <action>
    Implement `TopologicalMatchingService`. 
    It should depend on `TopologicalGraphRepository`.
    In `match_latent`, fetch all candidate graphs from the repository.
    Convert both the latent JSON dict and candidate JSON dicts into `networkx` graphs using `nx.node_link_graph` (or manual node/edge iteration).
    Use `networkx.algorithms.isomorphism.GraphMatcher` to check for subgraph isomorphisms.
    The score should reflect the size/quality of the matched subgraph.
    Return a list of `MatchResult` (person_id, score) sorted descending.
  </action>
  <verify>
    <automated>pytest apps/backend/tests/services/test_topological_matching_service.py</automated>
  </verify>
  <done>The service successfully identifies isomorphic subgraphs and scores them.</done>
</task>

</tasks>
