---
phase: "11-hexagonal-graphs"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "spikes/11_hexagonal_graph_spike.py"
  - "apps/backend/src/processing/graph_builder.py"
  - "apps/backend/tests/processing/test_graph_builder.py"
autonomous: true
requirements: ["PHASE11-01"]

must_haves:
  truths:
    - "A fingerprint can be converted into a topological graph representation."
    - "The graph structure correctly represents Voronoi neighbor connections."
  artifacts:
    - path: "apps/backend/src/processing/graph_builder.py"
      provides: "TopologicalGraphBuilder"
    - path: "spikes/11_hexagonal_graph_spike.py"
      provides: "Proof of concept for Subgraph Isomorphism matching."
  key_links:
    - from: "apps/backend/src/processing/graph_builder.py"
      to: "scipy.spatial.Voronoi"
      via: "geometric processing"
      pattern: "Voronoi"
---

<objective>
Validate the Hexagonal Graph Topology (Minutiae Stars) mathematical model through a spike, and implement the core `TopologicalGraphBuilder`.

Purpose: Shift from geometry-fragile Delaunay triplets to topology-robust Voronoi graphs, resisting elastic skin deformation.
Output: Spike validation script and the production domain builder class for fingerprint graphs.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
@.planning/phases/11-hexagonal-graphs/11-00-DISCOVERY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/backend/src/processing/vectorizer.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Graph Math Spike (Subgraph Isomorphism)</name>
  <files>spikes/11_hexagonal_graph_spike.py</files>
  <action>
    Create a standalone Python script that:
    1. Generates a synthetic fingerprint (list of minutiae: x, y, type).
    2. Computes the Voronoi diagram using `scipy.spatial.Voronoi` to find neighboring minutiae.
    3. Builds a `networkx` Graph (nodes=minutiae, edges=Voronoi boundaries).
    4. Simulates a "latent" fragment by taking a subgraph and applying elastic deformation (adding noise to x,y).
    5. Proves that `networkx.algorithms.isomorphism.GraphMatcher` can still find the latent subgraph within the full fingerprint graph based on topology (edges) and node attributes (minutia type).
  </action>
  <verify>
    <automated>python spikes/11_hexagonal_graph_spike.py</automated>
  </verify>
  <done>The script runs successfully and prints that an isomorphic subgraph was found despite geometric distortion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TopologicalGraphBuilder</name>
  <files>apps/backend/src/processing/graph_builder.py, apps/backend/tests/processing/test_graph_builder.py</files>
  <behavior>
    - Test: Builder takes a list of Minutia objects and returns a dict with `nodes` and `edges`.
    - Test: Isolated minutiae (no valid Voronoi neighbors) are handled gracefully.
    - Test: The resulting dict is JSON-serializable.
  </behavior>
  <action>
    Implement `TopologicalGraphBuilder` in `graph_builder.py`.
    It should compute the Voronoi neighbors for a given set of `MinutiaCandidate`s.
    Returns a standard dictionary: `{"nodes": [{"id": int, "type": str}], "edges": [{"source": int, "target": int}]}`.
    This output format is ready to be saved as JSONB in PostgreSQL.
  </action>
  <verify>
    <automated>pytest apps/backend/tests/processing/test_graph_builder.py</automated>
  </verify>
  <done>The builder correctly transforms minutiae into a node/edge JSON-friendly structure using Voronoi topology.</done>
</task>

</tasks>
