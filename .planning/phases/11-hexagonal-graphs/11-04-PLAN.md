---
phase: "11-hexagonal-graphs"
plan: "04"
type: execute
wave: 4
depends_on: ["11-03"]
files_modified:
  - "apps/backend/src/api/routers/topological_matching.py"
  - "apps/backend/src/main.py"
  - "apps/backend/tests/api/test_topological_matching_router.py"
autonomous: true
requirements: ["PHASE11-04"]

must_haves:
  truths:
    - "Users can enroll a fingerprint graph via the API."
    - "Users can submit a latent graph and get topological match results."
  artifacts:
    - path: "apps/backend/src/api/routers/topological_matching.py"
      provides: "FastAPI router endpoints"
  key_links:
    - from: "apps/backend/src/api/routers/topological_matching.py"
      to: "apps/backend/src/services/topological_matching_service.py"
      via: "FastAPI Depends()"
      pattern: "TopologicalMatchingService"
---

<objective>
Expose the Topological Matching capabilities via REST API.

Purpose: Allow external systems (or the frontend) to enroll fingerprint graphs and perform latent subgraph matching.
Output: FastAPI endpoints and E2E integration tests.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement API Router and E2E Test</name>
  <files>apps/backend/src/api/routers/topological_matching.py, apps/backend/src/main.py, apps/backend/tests/api/test_topological_matching_router.py</files>
  <behavior>
    - Test: POST `/api/v1/topology/enroll` accepts person_id and a graph JSON, saves it to DB, returns 201.
    - Test: POST `/api/v1/topology/match` accepts a latent graph JSON, returns a list of match candidates (200 OK).
  </behavior>
  <action>
    Create the `topological_matching` router.
    Implement the `enroll` and `match` endpoints, injecting `TopologicalGraphRepository` and `TopologicalMatchingService` via FastAPI's `Depends()`.
    Register the new router in `src/main.py`.
    Write integration tests that use `httpx.AsyncClient` to enroll a graph and then successfully match a subgraph against it.
  </action>
  <verify>
    <automated>pytest apps/backend/tests/api/test_topological_matching_router.py</automated>
  </verify>
  <done>The REST API is fully functional and successfully tested end-to-end.</done>
</task>

</tasks>
