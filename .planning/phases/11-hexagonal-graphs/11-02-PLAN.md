---
phase: "11-hexagonal-graphs"
plan: "02"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - "apps/backend/src/db/models.py"
  - "apps/backend/src/db/migrations/versions/*_add_topological_graphs_table.py"
  - "apps/backend/src/db/repositories/topological_graph_repository.py"
  - "apps/backend/tests/db/repositories/test_topological_graph_repository.py"
autonomous: true
requirements: ["PHASE11-02"]

must_haves:
  truths:
    - "System can store fingerprint topological graphs in PostgreSQL."
    - "Graphs are stored efficiently using JSONB without needing a dedicated GraphDB."
  artifacts:
    - path: "apps/backend/src/db/models.py"
      provides: "TopologicalGraph model"
    - path: "apps/backend/src/db/repositories/topological_graph_repository.py"
      provides: "Storage operations for graphs"
  key_links:
    - from: "apps/backend/src/db/repositories/topological_graph_repository.py"
      to: "apps/backend/src/db/models.py"
      via: "SQLAlchemy ORM"
      pattern: "TopologicalGraph"
---

<objective>
Implement the storage layer for Hexagonal Graph Topology.

Purpose: Store the topological graph of each enrolled fingerprint as a JSONB object in PostgreSQL, enabling fast in-memory matching later.
Output: SQLAlchemy model, Alembic migration, and Repository.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/backend/src/db/models.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add TopologicalGraph Model and Migration</name>
  <files>apps/backend/src/db/models.py</files>
  <action>
    Add a new `TopologicalGraph` SQLAlchemy model to `models.py`.
    Columns:
    - `id`: UUID (primary key, use `uuid7`)
    - `person_id`: String (indexed)
    - `graph_data`: JSONB (nullable=False)
    - `created_at`: DateTime

    Generate the Alembic migration file to apply this change to the database.
    (Use `cd apps/backend && alembic revision --autogenerate -m "add topological graphs table"`)
  </action>
  <verify>
    <automated>cd apps/backend && alembic upgrade head</automated>
  </verify>
  <done>The database schema is updated with the new table for storing JSONB graphs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TopologicalGraphRepository</name>
  <files>apps/backend/src/db/repositories/topological_graph_repository.py, apps/backend/tests/db/repositories/test_topological_graph_repository.py</files>
  <behavior>
    - Test: Repository can save a new TopologicalGraph (passing person_id and graph_data).
    - Test: Repository can fetch all graphs (for 1-to-N matching).
    - Test: Repository can fetch a graph by person_id.
  </behavior>
  <action>
    Create `TopologicalGraphRepository` to handle data access for the new model.
    Ensure dependency injection is supported (e.g., initialized with a SQLAlchemy `AsyncSession`).
  </action>
  <verify>
    <automated>pytest apps/backend/tests/db/repositories/test_topological_graph_repository.py</automated>
  </verify>
  <done>The repository handles CRUD operations for fingerprint graphs.</done>
</task>

</tasks>
