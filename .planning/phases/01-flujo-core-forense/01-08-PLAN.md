---
phase: 01-flujo-core-forense
plan: 08
type: execute
wave: 4
depends_on:
  - 01-07
files_modified:
  - "apps/frontend/src/App.tsx"
  - "apps/frontend/src/pages/Dashboard.tsx"
  - "apps/frontend/src/pages/ComparisonView.tsx"
autonomous: true
requirements:
  - AFIS-01
  - UI-01
  - UI-03
must_haves:
  truths:
    - "Forensic examiners can view their active cases"
    - "Examiners can visually compare a latent evidence against AFIS candidates side-by-side"
  artifacts:
    - path: "apps/frontend/src/pages/ComparisonView.tsx"
      provides: "Side-by-side visual comparison interface"
    - path: "apps/frontend/src/App.tsx"
      provides: "React Router configuration"
  key_links:
    - from: "apps/frontend/src/pages/ComparisonView.tsx"
      to: "/api/v1/matching"
      via: "React Query hooks"
      pattern: "useQuery"
---

<objective>
Implement the core UI forensic workflows in React.
Purpose: Provides the human-in-the-loop interface required for an examiner to review system-generated matches.
Output: React frontend configured with cases dashboard and comparison view.
</objective>

## Artifacts this phase produces
- See task actions for specific files, functions, and classes.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-flujo-core-forense/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: React Router & Dashboard</name>
  <read_first>apps/frontend/src/App.tsx, apps/frontend/src/pages/Dashboard.tsx</read_first>
  <action>
    Set up React Router v6 in `App.tsx` matching the new API routes. Create `Dashboard.tsx` to fetch (via React Query) and display the list of active cases, including a button to upload new latent evidence (per D-15).
  </action>
    <acceptance_criteria>
    - Dashboard renders the list of cases fetched from the backend.
- Automated: grep "React Router" apps/frontend/package.json || true
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Side-by-Side Comparison View</name>
  <read_first>apps/frontend/src/pages/ComparisonView.tsx</read_first>
  <action>
    Create `ComparisonView.tsx`. This component must display the uploaded latent fingerprint on the left and the top-k matched candidates on the right. Include explicit action buttons (Identificación, Exclusión, Inconcluso) that post the decision back to the API `/decisiones` endpoint, preventing any auto-approval.
  </action>
    <acceptance_criteria>
    - Examiner has a visual interface to make and record forensic decisions.
- Automated: grep "Identificación" apps/frontend/src/pages/ComparisonView.tsx || true
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> UI | XSS Protection on user-uploaded data rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Spoofing | Frontend Auth | mitigate | Rely on HttpOnly cookies or strictly managed JWT tokens in memory; do not store sensitive tokens in localStorage if avoidable. |
</threat_model>

<verification>
- The UI properly routes to `/cases/:id/compare`.
- The decisions explicitly send the chosen verdict to the API.
</verification>

<success_criteria>
- Frontend can be built successfully (`npm run build`).
- UI implements the strict human-in-the-loop requirement.
</success_criteria>

<output>
Create `.planning/phases/01-flujo-core-forense/01-08-SUMMARY.md` when done
</output>
