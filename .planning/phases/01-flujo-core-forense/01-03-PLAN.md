---
phase: 01-flujo-core-forense
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - "apps/backend/src/services/matching_service.py"
  - "apps/backend/src/api/routers/matching.py"
  - "apps/backend/src/api/routers/huellas_conocidas.py"
  - "apps/backend/scripts/benchmark_soco.py"
autonomous: true
requirements:
  - AFIS-01
  - AFIS-02
must_haves:
  truths:
    - "Fingerprint extraction runs on a background CPU thread"
    - "Vector similarity search uses HNSW to return Top-K candidates"
    - "Benchmark script can evaluate AFIS precision on SOCOFing"
  artifacts:
    - path: "apps/backend/src/services/matching_service.py"
      provides: "Orchestration between Evidencia, FingerprintService, and Vector DB"
    - path: "apps/backend/scripts/benchmark_soco.py"
      provides: "Standalone precision evaluation script"
  key_links:
    - from: "apps/backend/src/services/matching_service.py"
      to: "apps/backend/src/services/fingerprint_service.py"
      via: "ProcessPoolExecutor"
      pattern: "run_in_executor"
---

<objective>
Implement the biometric matching engine using `pgvector` HNSW and integrate CPU-bound biometric processing asynchronously.
Purpose: Prevents the API from blocking during heavy image processing and tests algorithm accuracy.
Output: Matching service, related routers, and SOCOFing benchmark script.
</objective>

## Artifacts this phase produces
- See task actions for specific files, functions, and classes.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-flujo-core-forense/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Matching Service Integration</name>
  <read_first>apps/backend/src/services/matching_service.py</read_first>
  <action>
    Create `MatchingService`. Implement logic to call the existing `FingerprintService.process` using `asyncio.get_running_loop().run_in_executor` passing the global pool (per D-11, D-12). Implement vector search logic executing L2 distance queries against the `pgvector` HNSW index, returning Top-K candidates.
  </action>
    <acceptance_criteria>
    - MatchingService cleanly bridges the CPU pipeline and vector queries.
- Automated: python -c "import apps.backend.src.services.matching_service"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Matching & Huellas Routers</name>
  <read_first>apps/backend/src/api/routers/matching.py, apps/backend/src/api/routers/huellas_conocidas.py</read_first>
  <action>
    Create routers with endpoints for `/huellas_conocidas` (upload known records) and `/matching` (trigger search for latent evidence). Inject `get_db` and the lifespan executor dependencies cleanly (per D-02).
  </action>
    <acceptance_criteria>
    - Routers exist exposing matching capabilities.
- Automated: python -c "import apps.backend.src.api.routers.matching"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement SOCOFing Benchmark</name>
  <read_first>apps/backend/scripts/benchmark_soco.py</read_first>
  <action>
    Create a standalone python script `benchmark_soco.py` that loads a sample of the SOCOFing dataset (if present locally), processes it via `FingerprintService`, inserts into a test DB, and measures Hit Rate (Rank-1, Rank-10) to satisfy requirement AFIS-02.
  </action>
    <acceptance_criteria>
    - Benchmark script is syntactically valid and executable.
- Automated: python -m py_compile apps/backend/scripts/benchmark_soco.py
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Extractor -> OS | CPU-bound processes handle unverified byte inputs (images) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Denial of Service | Matching Service | mitigate | Offload to `ProcessPoolExecutor` so the main event loop never blocks, preventing starvation. |
</threat_model>

<verification>
- Main event loop is not blocked by biometric image processing.
- pgvector HNSW L2 distance operator (`<->`) is used for queries.
</verification>

<success_criteria>
- Matching architecture handles async CPU offloading perfectly.
</success_criteria>

<output>
Create `.planning/phases/01-flujo-core-forense/01-03-SUMMARY.md` when done
</output>
