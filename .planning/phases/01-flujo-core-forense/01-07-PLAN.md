---
phase: 01-flujo-core-forense
plan: 07
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
  - 01-04
  - 01-05
  - 01-06
files_modified:
  - "apps/backend/src/main.py"
  - "apps/backend/src/api/rest.py"
autonomous: true
requirements:
  - AFIS-01
must_haves:
  truths:
    - "All modular routers are accessible under the /api/v1 prefix"
    - "The monolithic rest.py file is completely removed"
  artifacts:
    - path: "apps/backend/src/main.py"
      provides: "FastAPI application entrypoint with lifespan manager"
  key_links:
    - from: "apps/backend/src/main.py"
      to: "apps/backend/src/api/routers/*.py"
      via: "app.include_router"
      pattern: "include_router"
---

<objective>
Wire all the newly created REST routers into the main FastAPI application and remove the legacy monolith.
Purpose: Completes the architectural refactoring (D-02, D-03) and activates the DI lifespan (D-04).
Output: Clean, modular main.py and deletion of rest.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-flujo-core-forense/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble FastAPI App & Lifespan</name>
  <files>apps/backend/src/main.py</files>
  <action>
    Modify `main.py` to initialize the FastAPI app with the async `lifespan` manager from `dependencies.py`. Register all global exception handlers from `errors.py`. Include all routers (`auth`, `cases`, `evidencias`, `huellas_conocidas`, `matching`, `decisiones`, `dictamenes`, `auditoria`) using the prefix `/api/v1` (per D-03).
  </action>
  <verify>
    <automated>python -c "import apps.backend.src.main"</automated>
  </verify>
  <done>App initializes without import errors and registers all routes.</done>
</task>

<task type="auto">
  <name>Task 2: Monolith Teardown</name>
  <files>apps/backend/src/api/rest.py</files>
  <action>
    Delete the monolithic `rest.py` file completely, as its functionality has been successfully distributed across the modular routers. Ensure no dangling imports remain across the codebase pointing to `rest.py`.
  </action>
  <verify>
    <automated>test ! -f apps/backend/src/api/rest.py</automated>
  </verify>
  <done>rest.py is removed from the file system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Framework -> Router | Central dispatch routing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Information Disclosure | Exception Handlers | mitigate | Global handlers must not leak internal stack traces or DB internal errors to the client. |
</threat_model>

<verification>
- The application starts cleanly.
- The Swagger UI (`/docs`) correctly displays the segmented V1 endpoints.
</verification>

<success_criteria>
- The backend is fully operational on the new architecture.
</success_criteria>

<output>
Create `.planning/phases/01-flujo-core-forense/01-07-SUMMARY.md` when done
</output>
