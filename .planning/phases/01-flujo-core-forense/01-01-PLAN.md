---
phase: 01-flujo-core-forense
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "apps/backend/alembic.ini"
  - "apps/backend/src/db/migrations/env.py"
  - "apps/backend/src/db/models.py"
  - "apps/backend/src/api/errors.py"
  - "apps/backend/src/api/dependencies.py"
autonomous: false
requirements:
  - AFIS-01
must_haves:
  truths:
    - "Alembic is configured as the sole migration tool"
    - "Database models are defined using UUIDv7 for primary keys"
    - "Vector table defines an HNSW index via pgvector"
  artifacts:
    - path: "apps/backend/src/db/models.py"
      provides: "Base declarative models for the domain"
    - path: "apps/backend/src/api/dependencies.py"
      provides: "Lifespan and DB dependency injection hooks"
  key_links:
    - from: "apps/backend/src/db/migrations/env.py"
      to: "apps/backend/src/db/models.py"
      via: "target_metadata"
      pattern: "target_metadata"
---

<objective>
Setup the core persistence and error handling foundation for the backend.
Purpose: Replaces naive schema generation with Alembic, introduces HNSW vector indices, and establishes the DI infrastructure (lifespan) required for CPU-bound tasks.
Output: Core DB models, Alembic configuration, and global FastAPI exception handlers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-flujo-core-forense/01-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify `uuid6` Package Legitimacy</name>
  <what-built>Requested human verification of PyPI package `uuid6` (flagged as [ASSUMED] during research)</what-built>
  <how-to-verify>
    Verify on pypi.org/project/uuid6 that the package is legitimate, not typosquatting, and safe to use for UUIDv7 generation.
  </how-to-verify>
  <resume-signal>Type "approved" if legitimate and safe to install</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Setup Alembic & SQLAlchemy Models</name>
  <files>apps/backend/alembic.ini, apps/backend/src/db/migrations/env.py, apps/backend/src/db/models.py</files>
  <action>
    Initialize Alembic in `apps/backend/src/db/migrations`. In `models.py`, define the SQLAlchemy ORM models (Cases, Evidences, AuditLog). Use `uuid6.uuid7()` as the default for all `UUID` primary keys (per D-07). For the fingerprint vectors table, define an `embedding` column using `pgvector.sqlalchemy.Vector` and include an HNSW index definition in the table arguments (per D-08). Never use `create_all` (D-06).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; alembic current</automated>
  </verify>
  <done>Models are defined with UUIDv7 and Alembic env.py loads the metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Global Errors & DI Lifespan</name>
  <files>apps/backend/src/api/errors.py, apps/backend/src/api/dependencies.py</files>
  <action>
    Create `errors.py` with custom exceptions: `ForensicError`, `ValidationError`, `IntegrityError` (per D-05).
    Create `dependencies.py` providing a `get_db` generator for SQLAlchemy sessions and an async `lifespan` context manager that initializes a global `ProcessPoolExecutor` for CPU-bound fingerprint tasks, disposing it on shutdown (per D-04).
  </action>
  <verify>
    <automated>python -c "from apps.backend.src.api.dependencies import get_db; print(get_db)"</automated>
  </verify>
  <done>Exception classes exist and lifespan manager is defined.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> DB | Model layer acts as the boundary preventing unescaped queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | DB Models | mitigate | Use SQLAlchemy ORM strictly; no string-formatted SQL queries. |
| T-01-SC | Tampering | npm/pip installs | mitigate | slopcheck + blocking human checkpoint for `uuid6` ([ASSUMED]). |
</threat_model>

<verification>
- UUIDv7 is explicitly configured for PK generation.
- pgvector HNSW index is defined in SQLAlchemy models.
</verification>

<success_criteria>
- Alembic runs successfully and recognizes models.
- Custom exceptions and dependency injection hooks are cleanly separated.
</success_criteria>

<output>
Create `.planning/phases/01-flujo-core-forense/01-01-SUMMARY.md` when done
</output>
