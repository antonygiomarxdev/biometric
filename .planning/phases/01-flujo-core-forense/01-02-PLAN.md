---
phase: 01-flujo-core-forense
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - "apps/backend/src/db/migrations/versions/001_initial_schema.py"
  - "apps/backend/src/db/migrations/versions/002_seed_data.py"
  - "apps/backend/src/services/audit_service.py"
  - "apps/backend/src/api/routers/auditoria.py"
autonomous: true
requirements:
  - AFIS-01
must_haves:
  truths:
    - "Base DB schema is generated via Alembic"
    - "Seed data (Roles, Default Users) is automatically inserted"
    - "Audit records form a cryptographically unbroken hash chain"
  artifacts:
    - path: "apps/backend/src/services/audit_service.py"
      provides: "Audit log generation with SHA-256 chaining"
  key_links:
    - from: "apps/backend/src/services/audit_service.py"
      to: "apps/backend/src/db/models.py"
      via: "SELECT FOR UPDATE lock"
      pattern: "with_for_update"
---

<objective>
Generate the initial database schema and seed data, and implement the immutable audit log service.
Purpose: Ensures all state changes are forensically tracked with an unbreakable hash chain and initializes the system state.
Output: Initial Alembic migrations, Audit Service, and Auditoria router.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-flujo-core-forense/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Schema & Seed Data Migrations</name>
  <files>apps/backend/src/db/migrations/versions/001_initial_schema.py, apps/backend/src/db/migrations/versions/002_seed_data.py</files>
  <action>
    Run Alembic to generate `001_initial_schema.py` based on the models created in Plan 01. Then generate an empty revision `002_seed_data.py` and implement raw SQL inserts (`op.execute`) for standard roles (Admin, Perito), default users, and crime types (per D-10).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; alembic upgrade head</automated>
  </verify>
  <done>Database contains the schema and basic seed rows.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Audit Service</name>
  <files>apps/backend/src/services/audit_service.py, apps/backend/src/api/routers/auditoria.py</files>
  <action>
    Implement `AuditService.log_event(session, user_id, action, payload)`. Must query the most recent log entry using `with_for_update()` to lock it, then calculate `SHA-256(prev_hash + stringified_payload)` and insert the new log (per D-09). Create `auditoria.py` router with an endpoint to fetch the audit log history.
  </action>
  <verify>
    <automated>python -c "import apps.backend.src.services.audit_service"</automated>
  </verify>
  <done>Audit service provides serialized hash chaining method.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service -> Audit Log | Ensures logs cannot be retroactively tampered with |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | Audit Service | mitigate | `with_for_update` prevents race conditions; cryptographic hashing binds sequential records. |
</threat_model>

<verification>
- Audit chain explicitly hashes the previous record's hash.
- Alembic upgrade populates the database correctly.
</verification>

<success_criteria>
- Seed data migration executes cleanly.
- Audit service prevents concurrency race conditions during log writing.
</success_criteria>

<output>
Create `.planning/phases/01-flujo-core-forense/01-02-SUMMARY.md` when done
</output>
