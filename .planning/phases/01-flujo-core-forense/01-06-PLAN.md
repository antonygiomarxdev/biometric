---
phase: 01-flujo-core-forense
plan: 06
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - "apps/backend/src/services/auth_service.py"
  - "apps/backend/src/api/routers/auth.py"
autonomous: true
requirements:
  - AFIS-01
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - "JWT tokens are issued upon successful login"
    - "Role-based access control (RBAC) verifies user permissions"
  artifacts:
    - path: "apps/backend/src/services/auth_service.py"
      provides: "JWT encoding/decoding and password verification"
    - path: "apps/backend/src/api/routers/auth.py"
      provides: "Login endpoint and security dependency injections"
  key_links:
    - from: "apps/backend/src/api/routers/auth.py"
      to: "FastAPI endpoints"
      via: "Depends(get_current_user)"
      pattern: "get_current_user"
---

<objective>
Establish foundational security via JWT authentication and basic role-based access control.
Purpose: Ensures only authorized forensic personnel (Admin, Perito) can interact with the system.
Output: Auth service handling hashing and JWT, and an Authentication router.
</objective>

## Artifacts this phase produces
- See task actions for specific files, functions, and classes.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-flujo-core-forense/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth Service Implementation</name>
  <read_first>apps/backend/src/services/auth_service.py</read_first>
  <action>
    Implement JWT token generation and validation using `python-jose` (or `PyJWT`) and password hashing using `passlib` (bcrypt). Create dependency functions `get_current_user` and role-check closures (e.g., `RequireRole("Perito")`).
  </action>
    <acceptance_criteria>
    - JWT tokens are generated and decoded correctly.
- Automated: python -c "import apps.backend.src.services.auth_service"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Auth Router & Login Endpoint</name>
  <read_first>apps/backend/src/api/routers/auth.py</read_first>
  <action>
    Create `/auth/login` endpoint using `OAuth2PasswordRequestForm`. Validate credentials against the database and issue the JWT access token.
  </action>
    <acceptance_criteria>
    - Clients can exchange credentials for a JWT.
- Automated: python -c "import apps.backend.src.api.routers.auth"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Authentication layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Spoofing | Auth Service | mitigate | Validate passwords with bcrypt; enforce secure JWT signatures. |
</threat_model>

<verification>
- JWT token expiration is enforced.
- Passwords are never stored in plaintext.
</verification>

<success_criteria>
- Dependency hooks correctly reject unauthenticated requests with HTTP 401.
</success_criteria>

<output>
Create `.planning/phases/01-flujo-core-forense/01-06-SUMMARY.md` when done
</output>
