---
phase: 01-flujo-core-forense
plan: 05
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - "apps/backend/src/services/pdf_generator.py"
  - "apps/backend/src/api/routers/dictamenes.py"
autonomous: true
requirements:
  - AFIS-01
  - UI-05
must_haves:
  truths:
    - "A legal PDF/A document can be generated for a matched case"
    - "The PDF is cryptographically signed using HMAC-SHA256"
  artifacts:
    - path: "apps/backend/src/services/pdf_generator.py"
      provides: "WeasyPrint integration and HMAC generation"
    - path: "apps/backend/src/api/routers/dictamenes.py"
      provides: "PDF download endpoint"
  key_links:
    - from: "apps/backend/src/services/pdf_generator.py"
      to: "apps/backend/src/api/routers/dictamenes.py"
      via: "Response generation"
      pattern: "StreamingResponse"
---

<objective>
Implement automated legal dictamen (report) generation in PDF/A format.
Purpose: Replaces manual paperwork by producing a signed, auditable document based on system metadata.
Output: PDF Generator service utilizing WeasyPrint and a router to serve the files.
</objective>

## Artifacts this phase produces
- See task actions for specific files, functions, and classes.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-flujo-core-forense/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement PDFGeneratorService</name>
  <read_first>apps/backend/src/services/pdf_generator.py</read_first>
  <action>
    Create `PDFGeneratorService`. Build a simple HTML template string injected with case metadata. Use `weasyprint.HTML` to render it to PDF bytes (per D-13). Generate an HMAC-SHA256 signature using a server secret (from env vars) and append/embed it into the PDF metadata or a footer, alongside a timestamp (per D-14).
  </action>
    <acceptance_criteria>
    - Service can convert dict dictionaries into signed PDF bytes.
- Automated: python -c "import apps.backend.src.services.pdf_generator"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Dictamenes Router</name>
  <read_first>apps/backend/src/api/routers/dictamenes.py</read_first>
  <action>
    Create `/dictamenes` router. Provide a `GET` or `POST` endpoint that accepts a case ID, retrieves the decision and evidence metadata from the database, invokes the PDF generator, and returns the binary data via `Response` (with `application/pdf` media type).
  </action>
    <acceptance_criteria>
    - Dictamen generation is exposed via REST.
- Automated: python -c "import apps.backend.src.api.routers.dictamenes"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System -> PDF Export | Outbound documents carrying forensic authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | PDF Generation | mitigate | Include HMAC-SHA256 signature calculated with server secret to verify authenticity later. |
</threat_model>

<verification>
- WeasyPrint successfully runs without blocking event loop (it may be CPU intensive, wrap in `run_in_executor` if necessary).
- Signature validation is reproducible given the secret.
</verification>

<success_criteria>
- The endpoint correctly returns a valid PDF file.
</success_criteria>

<output>
Create `.planning/phases/01-flujo-core-forense/01-05-SUMMARY.md` when done
</output>
