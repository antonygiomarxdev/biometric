---
phase: 01-flujo-core-forense
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - "apps/backend/src/api/routers/cases.py"
  - "apps/backend/src/api/routers/evidencias.py"
  - "apps/backend/src/api/routers/decisiones.py"
autonomous: true
requirements:
  - AFIS-01
must_haves:
  truths:
    - "Cases can be created and retrieved"
    - "Latent evidence images can be uploaded and attached to cases"
    - "Forensic examiners can submit explicit visual comparison decisions"
  artifacts:
    - path: "apps/backend/src/api/routers/cases.py"
      provides: "REST endpoints for managing investigative cases"
    - path: "apps/backend/src/api/routers/decisiones.py"
      provides: "Endpoint for examiner matching verdicts"
  key_links:
    - from: "apps/backend/src/api/routers/decisiones.py"
      to: "apps/backend/src/services/audit_service.py"
      via: "Dependency logic"
      pattern: "log_event"
---

<objective>
Implement the core REST resource routers to break apart the monolithic architecture.
Purpose: Provides structured, maintainable endpoints for cases, evidence, and examiner decisions per D-02.
Output: FastApi routers for Cases, Evidencias, and Decisiones.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-flujo-core-forense/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cases & Evidencias Routers</name>
  <files>apps/backend/src/api/routers/cases.py, apps/backend/src/api/routers/evidencias.py</files>
  <action>
    Create `/cases` and `/evidencias` routers using standard FastAPI CRUD patterns. Inject the DB session via `Depends(get_db)`. For evidences, support `UploadFile` to handle image ingestion.
  </action>
  <verify>
    <automated>python -c "import apps.backend.src.api.routers.cases"</automated>
  </verify>
  <done>Routers for Cases and Evidencias are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Decisiones Router</name>
  <files>apps/backend/src/api/routers/decisiones.py</files>
  <action>
    Create `/decisiones` router. Expose an endpoint that allows the `Perito` role to explicitly confirm or reject a match (Identificación, Exclusión, Inconcluso). This endpoint must also log the decision via the `AuditService` hash chain (per explicit spec that system never auto-approves).
  </action>
  <verify>
    <automated>python -c "import apps.backend.src.api.routers.decisiones"</automated>
  </verify>
  <done>Decision router correctly delegates to database and audit logging.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet -> API | File uploads for evidence images |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Elevation of Privilege | Decisiones Router | mitigate | Role checks injected via FastAPI Depends to ensure only 'Perito' can decide. |
| T-01-05 | Spoofing | Evidencias Router | mitigate | Basic validation of image MIME types and file extension on upload. |
</threat_model>

<verification>
- Monolithic API logic is cleanly moved into discrete resource files.
- File uploads are handled securely via `UploadFile`.
</verification>

<success_criteria>
- CRUD operations for core entities are fully defined and type-checked via Pydantic.
</success_criteria>

<output>
Create `.planning/phases/01-flujo-core-forense/01-04-SUMMARY.md` when done
</output>
