---
phase: 03-global-compliance-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/core/compliance/strategy.py
  - apps/backend/src/core/compliance/base.py
  - apps/backend/src/core/compliance/extreme.py
  - apps/backend/src/core/config.py
  - apps/backend/src/core/compliance/factory.py
autonomous: true
requirements: [COMPLIANCE-01]
must_haves:
  truths:
    - "System can instantiate different compliance strategies based on configuration"
    - "Base and Extreme privacy strategies exist with defined behaviors"
  artifacts:
    - path: "apps/backend/src/core/compliance/strategy.py"
      provides: "IComplianceStrategy protocol/ABC"
      contains: "class IComplianceStrategy"
    - path: "apps/backend/src/core/compliance/extreme.py"
      provides: "ExtremePrivacyStrategy implementation"
    - path: "apps/backend/src/core/config.py"
      provides: "Compliance configuration"
      contains: "compliance_strategy"
  key_links:
    - from: "apps/backend/src/core/compliance/factory.py"
      to: "apps/backend/src/core/config.py"
      via: "Config injection or read"
      pattern: "config.compliance_strategy"
---

<objective>
Define the core `IComplianceStrategy` interface and initial implementations (Base and Extreme) to support cross-cutting jurisdiction rules. Tie it into the global configuration.

Purpose: Foundation for dynamic compliance enforcement across logging, AI, and storage per ADR 007.
Output: Core compliance interfaces, factory, and configuration updates.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ksante/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/adr/007-global-compliance-architecture.md
@apps/backend/src/core/config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define IComplianceStrategy & Implementations</name>
  <files>apps/backend/src/core/compliance/strategy.py, apps/backend/src/core/compliance/base.py, apps/backend/src/core/compliance/extreme.py, apps/backend/tests/core/compliance/test_strategy.py</files>
  <behavior>
    - Test 1: BaseStrategy methods return default/no-op behaviors (e.g. requires_client_side_encryption is False).
    - Test 2: ExtremePrivacyStrategy returns strict behaviors (e.g. requires_client_side_encryption is True).
  </behavior>
  <action>Create `IComplianceStrategy` (using `typing.Protocol` or `abc.ABC` in `strategy.py`) defining: `scrub_pii(text: str) -> str`, `requires_client_side_encryption() -> bool`, `get_audit_strictness() -> str`, `anonymize_prompt_data(data: dict) -> dict`, `deanonymize_prompt_data(data: dict) -> dict`. Implement `BaseStrategy` (no-ops or minimal) in `base.py` and `ExtremePrivacyStrategy` (forces encryption, strong scrubbing) in `extreme.py`.</action>
  <verify>
    <automated>cd apps/backend && pytest tests/core/compliance/test_strategy.py</automated>
  </verify>
  <done>Interfaces and strategies are defined, typed strictly, and pass unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Configure and Factory</name>
  <files>apps/backend/src/core/config.py, apps/backend/src/core/compliance/factory.py</files>
  <action>Add `compliance_strategy: str` (default "base") to `Config` in `apps/backend/src/core/config.py`. Create `apps/backend/src/core/compliance/factory.py` with `get_compliance_strategy(name: str) -> IComplianceStrategy` that returns the correct strategy instance based on the string name. Use Clean Architecture principles.</action>
  <verify>
    <automated>cd apps/backend && python -c "from src.core.compliance.factory import get_compliance_strategy; get_compliance_strategy('extreme')"</automated>
  </verify>
  <done>Factory successfully reads configuration strings and instantiates appropriate strategy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app→config | Configuration defines the compliance level, must not be downgraded at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | config.py / factory | mitigate | Strategy instantiation based on environment variables; not overridable via API inputs |
</threat_model>

<verification>
Check that `ExtremePrivacyStrategy` requires client-side encryption.
Check that `get_compliance_strategy` falls back safely or raises clear error for invalid strategies.
</verification>

<success_criteria>
- Strict typing enabled and passes `pyright`.
- Architecture allows adding new strategies without touching existing ones (Open/Closed principle).
- Clean Code practices followed.
</success_criteria>

<output>
Create `.planning/phases/03-global-compliance-core/03-01-SUMMARY.md` when done
</output>
