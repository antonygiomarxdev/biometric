---
phase: 03-global-compliance-core
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - apps/backend/src/core/compliance/logger.py
  - apps/backend/src/main.py
autonomous: true
requirements: [COMPLIANCE-02]
must_haves:
  truths:
    - "Application logs do not contain raw PII when Extreme strategy is active"
  artifacts:
    - path: "apps/backend/src/core/compliance/logger.py"
      provides: "PII Scrubbing Formatter"
  key_links:
    - from: "apps/backend/src/main.py"
      to: "apps/backend/src/core/compliance/logger.py"
      via: "logging config"
      pattern: "logging\\.set.*"
---

<objective>
Implement a custom logging formatter that uses the active compliance strategy to intercept and scrub PII (like UUIDs, emails, names) from all logs before they are written.

Purpose: Prevent data leaks in system logs per ADR 007.
Output: Custom formatter and updated global logging configuration.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ksante/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/adr/007-global-compliance-architecture.md
@apps/backend/src/main.py
@apps/backend/src/core/compliance/factory.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Log Formatter Implementation</name>
  <files>apps/backend/src/core/compliance/logger.py, apps/backend/tests/core/compliance/test_logger.py</files>
  <behavior>
    - Test 1: PIIFormatter leaves logs unchanged if active strategy is BaseStrategy.
    - Test 2: PIIFormatter scrubs UUIDs and emails using regex if active strategy is ExtremePrivacyStrategy.
  </behavior>
  <action>Create a custom `logging.Formatter` or `logging.Filter` called `ComplianceLogFormatter`. It should retrieve the active compliance strategy (via factory and config) and pass `record.msg` through `strategy.scrub_pii(msg)`. Update `ExtremePrivacyStrategy` to implement `scrub_pii` using regex for UUIDs and emails (replacing with `***`).</action>
  <verify>
    <automated>cd apps/backend && pytest tests/core/compliance/test_logger.py</automated>
  </verify>
  <done>Formatter intercepts log messages and alters them correctly based on the active strategy.</done>
</task>

<task type="auto">
  <name>Task 2: Inject Scrubber Globally</name>
  <files>apps/backend/src/main.py</files>
  <action>In `apps/backend/src/main.py` (and any logger setup functions), configure the root logger to use `ComplianceLogFormatter`. Ensure it handles FastAPI and uvicorn logs where applicable, ensuring no PII leaks into stdout.</action>
  <verify>
    <automated>cd apps/backend && python -c "import src.main"</automated>
  </verify>
  <done>Global logging configuration is wired to use the compliance formatter without crashing the startup sequence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| log→stdout | Raw logs crossing into standard output/files where observability tools scrape them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Information Disclosure | logging | mitigate | Scrub PII directly in the logging pipeline before serialization |
</threat_model>

<verification>
A log containing an email address should be printed with `***` if `COMPLIANCE_STRATEGY=extreme`.
</verification>

<success_criteria>
- Strict typing and Clean Architecture compliance.
- No recursive logging loops.
- `pyright` checks pass.
</success_criteria>

<output>
Create `.planning/phases/03-global-compliance-core/03-02-SUMMARY.md` when done
</output>
