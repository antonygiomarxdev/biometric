---
phase: 03-global-compliance-core
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - apps/backend/src/core/config.py
  - apps/backend/src/core/compliance/encryption.py
  - apps/backend/src/storage/object_storage.py
autonomous: true
requirements: [COMPLIANCE-04]
user_setup:
  - service: cryptography
    why: "Client-side encryption requires a Fernet key"
    env_vars:
      - name: ENCRYPTION_KEY
        source: "Generated via python -c 'from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())'"
must_haves:
  truths:
    - "System encrypts object storage uploads when strategy requires it"
    - "System transparently decrypts object storage downloads"
  artifacts:
    - path: "apps/backend/src/core/compliance/encryption.py"
      provides: "EncryptionService"
    - path: "apps/backend/src/storage/object_storage.py"
      provides: "MinIO client wrapper with encryption hooks"
  key_links:
    - from: "apps/backend/src/storage/object_storage.py"
      to: "apps/backend/src/core/compliance/encryption.py"
      via: "upload/download hooks"
      pattern: "encrypt.*|decrypt.*"
---

<objective>
Implement client-side encryption for object storage (MinIO). Biometric images must be encrypted using `cryptography.fernet` (AES) before upload, and decrypted upon download, if the active compliance strategy mandates it.

Purpose: Guarantee data-at-rest protection at the application layer independent of the storage provider per ADR 007.
Output: Encryption service and transparent object storage hooks.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ksante/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/adr/007-global-compliance-architecture.md
@apps/backend/src/storage/object_storage.py
@apps/backend/src/core/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encryption Key Configuration</name>
  <files>apps/backend/src/core/config.py</files>
  <action>Add `storage_encryption_key: str` to `Config` in `apps/backend/src/core/config.py`, pulling from `STORAGE_ENCRYPTION_KEY` env var. It should default to an empty string but be required if the Extreme strategy is active.</action>
  <verify>
    <automated>cd apps/backend && python -c "from src.core.config import config; hasattr(config, 'storage_encryption_key')"</automated>
  </verify>
  <done>Configuration supports an encryption key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encryption Service</name>
  <files>apps/backend/src/core/compliance/encryption.py, apps/backend/tests/core/compliance/test_encryption.py</files>
  <behavior>
    - Test 1: EncryptionService encrypts raw bytes and decrypts them back perfectly using Fernet.
    - Test 2: Fails securely if instantiated with an invalid key.
  </behavior>
  <action>Create `EncryptionService` in `apps/backend/src/core/compliance/encryption.py` utilizing `cryptography.fernet.Fernet`. It needs methods `encrypt(data: bytes) -> bytes` and `decrypt(data: bytes) -> bytes`. Rely on the `storage_encryption_key` from config.</action>
  <verify>
    <automated>cd apps/backend && pytest tests/core/compliance/test_encryption.py</automated>
  </verify>
  <done>Data can be encrypted and decrypted symmetrically.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate into ObjectStorage</name>
  <files>apps/backend/src/storage/object_storage.py</files>
  <action>Modify `apps/backend/src/storage/object_storage.py` (which likely has `upload_file` and `download_file` or similar). Inject `IComplianceStrategy` and `EncryptionService`. Before `put_object`, if `strategy.requires_client_side_encryption()` is true, pass the bytes through `encryption_service.encrypt()`. Upon retrieval, pass through `decrypt()`. Keep it transparent to the rest of the application.</action>
  <verify>
    <automated>cd apps/backend && python -c "import src.storage.object_storage"</automated>
  </verify>
  <done>Object storage seamlessly encrypts/decrypts based on strategy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app→minio | Storing forensic images in MinIO object storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | object_storage | mitigate | Encrypt blobs application-side before transport using symmetric AES |
</threat_model>

<verification>
When Extreme strategy is enabled, uploaded file content should be encrypted ciphertext (not matching raw jpeg bytes).
</verification>

<success_criteria>
- Strict typing and Clean Architecture compliance.
- No plaintext leaks to storage when encryption is mandated.
- `pyright` checks pass.
</success_criteria>

<output>
Create `.planning/phases/03-global-compliance-core/03-04-SUMMARY.md` when done
</output>
