---
phase: 11-ridge-graph-topology
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - "apps/backend/docker-compose.gpu.yml"
  - "apps/backend/src/db/nebula_repository.py"
  - "apps/backend/pyproject.toml"
autonomous: true
requirements:
  - GRAPH-003
must_haves:
  truths:
    - "NebulaGraph is available via Docker Compose."
    - "RidgeGraphs are mapped into NebulaGraph Vertices and Edges."
    - "Subgraph Isomorphism queries return exact topological matches."
  artifacts:
    - path: "apps/backend/src/db/nebula_repository.py"
      provides: "NebulaRepository for topological graph matching."
---

<objective>
Implement the Fine Matcher using NebulaGraph.

Purpose:
Once Qdrant filters the database down to 1,000 suspects, NebulaGraph performs the heavy lifting: Subgraph Isomorphism. It asks: "Does the crime-scene graph exactly fit into the topology of any of these 1,000 known graphs?". Because it evaluates the *connections* rather than exact distances, it is 100% immune to elastic skin stretching.

Output: `NebulaRepository` and Cypher/nGQL matching queries.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>
<task type="auto">
  <name>Add NebulaGraph to Stack</name>
  <files>apps/backend/docker-compose.gpu.yml, pyproject.toml</files>
  <action>Add NebulaGraph services (graphd, metad, storaged) to Docker Compose and `nebula3-python` client.</action>
  <verify>Services start correctly.</verify>
  <done>NebulaGraph is available locally.</done>
</task>

<task type="auto">
  <name>Implement NebulaRepository</name>
  <files>apps/backend/src/db/nebula_repository.py</files>
  <action>
Implement `insert_graph` (creates VERTICES for minutiae and EDGES for ridges) and `match_subgraph` (executes the topological matching nGQL query restricted to a list of candidate `person_id`s).
  </action>
  <verify>Graph insertion and traversal queries pass.</verify>
  <done>Fine Matcher persistence layer established.</done>
</task>
</tasks>
