---
phase: 11-ridge-graph-topology
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - "apps/backend/docker-compose.gpu.yml"
  - "apps/backend/src/db/qdrant_repository.py"
  - "apps/backend/src/processing/graph_embedder.py"
  - "apps/backend/pyproject.toml"
autonomous: true
requirements:
  - GRAPH-002
must_haves:
  truths:
    - "Qdrant is available via Docker Compose."
    - "A RidgeGraph is converted into a dense vector (Graph Embedding) for rapid O(log N) searches."
  artifacts:
    - path: "apps/backend/src/db/qdrant_repository.py"
      provides: "QdrantRepository for vector indexing/search."
---

<objective>
Implement the Coarse Matcher using Qdrant.

Purpose:
To scale to 50M fingerprints, we cannot run Subgraph Isomorphism on every print. We must first filter down to the Top 1,000 candidates. We will use a fast "Graph Embedding" algorithm to convert the `RidgeGraph` into a dense vector, and store it in Qdrant (a highly scalable, rust-based Vector DB).

Output: `GraphEmbedder` and `QdrantRepository`.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>
<task type="auto">
  <name>Add Qdrant to Stack</name>
  <files>apps/backend/docker-compose.gpu.yml, pyproject.toml</files>
  <action>Add `qdrant/qdrant` to Docker Compose and `qdrant-client` to Python dependencies.</action>
  <verify>docker compose up works.</verify>
  <done>Qdrant is ready.</done>
</task>

<task type="auto">
  <name>Implement GraphEmbedder</name>
  <files>apps/backend/src/processing/graph_embedder.py</files>
  <action>
Implement an algorithm to create a fixed-size vector from the `RidgeGraph` (e.g., node degree histograms, edge length distributions). This vector represents the macro-topology of the graph.
  </action>
  <verify>Embedding produces a consistent, fixed-size vector for a given graph.</verify>
  <done>Graph translates to Vector.</done>
</task>

<task type="auto">
  <name>Implement QdrantRepository</name>
  <files>apps/backend/src/db/qdrant_repository.py</files>
  <action>
Implement `insert_embedding` and `search_top_k` methods using the official Qdrant Python client.
  </action>
  <verify>Tests confirm vector retrieval works against a local/testcontainers Qdrant instance.</verify>
  <done>Coarse Matcher persistence layer established.</done>
</task>
</tasks>
