---
phase: 11-ridge-graph-topology
plan: 04
type: execute
wave: 4
depends_on: [11-03]
files_modified:
  - "apps/backend/src/services/polyglot_matching_service.py"
  - "apps/backend/src/api/routers/latent_search.py"
  - "apps/backend/tests/integration/test_polyglot_e2e.py"
autonomous: true
requirements:
  - GRAPH-004
must_haves:
  truths:
    - "PolyglotMatchingService orchestrates the full Funnel: Extraction -> Qdrant (Top K) -> NebulaGraph (Exact Match) -> Postgres (Details)."
    - "The legacy RAG matching service is deprecated or removed following the No Legacy mandate."
  artifacts:
    - path: "apps/backend/src/services/polyglot_matching_service.py"
      provides: "The master orchestrator for the AFIS."
---

<objective>
Orchestrate the Polyglot Architecture into a single, cohesive Matching Service.

Purpose:
Bring together the Graph Extractor, Qdrant Coarse Matcher, NebulaGraph Fine Matcher, and PostgreSQL metadata store. Provide clean `enroll` and `search` endpoints that abstract this massive scale from the API routers. Ensure E2E tests prove the elastic deformation immunity.

Output: `PolyglotMatchingService`, updated API routers, and definitive Elasticity Tests.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>
<task type="auto">
  <name>Implement PolyglotMatchingService</name>
  <files>apps/backend/src/services/polyglot_matching_service.py</files>
  <action>
Create the orchestrator.
- `enroll`: Extract Graph -> Save to Nebula -> Embed Graph -> Save to Qdrant -> Save metadata to Postgres.
- `search`: Extract Graph -> Embed Graph -> Query Qdrant for Top 1000 IDs -> Pass IDs and Latent Graph to Nebula -> Nebula returns the single match.
  </action>
  <verify>Service strictly routes data without leaking DB concerns.</verify>
  <done>The enterprise AFIS engine is complete.</done>
</task>

<task type="auto">
  <name>The "No Legacy" Purge (Postgres Vector Cleanup)</name>
  <files>apps/backend/src/processing/vectorizer.py, apps/backend/src/db/models.py, apps/backend/alembic/versions/*, apps/backend/docker-compose.gpu.yml, apps/backend/src/core/config.py</files>
  <action>
Aggressively delete the deprecated Phase 10 engine:
- Delete `RagTripletVectorizer` and `OnnxPatchFilter`.
- Drop the `rag_vector_chunks` table via a new Alembic migration.
- Remove `pgvector` dependencies from the Postgres container in `docker-compose.gpu.yml` (since Qdrant handles vectors now).
- Clean up obsolete env vars (`USE_AI_PATCH_FILTER`, `AI_PATCH_THRESHOLD`) from `config.py`.
  </action>
  <verify>Project compiles, tests pass, and zero references to Phase 10 logic remain.</verify>
  <done>Phase 10 codebase entirely eradicated per the No Legacy mandate.</done>
</task>

<task type="auto">
  <name>E2E Elasticity Deformation Test</name>
  <files>apps/backend/tests/integration/test_polyglot_e2e.py</files>
  <action>
The ultimate test: Enroll a real fingerprint. Take a crop of that fingerprint and mathematically distort it (non-linear stretch: X*1.4, Y*0.8 + noise). Pass it to `search`. The Polyglot Service MUST find the original fingerprint, proving that Topology over Geometry works.
  </action>
  <verify>Test passes, proving the system is superior to Phase 10.</verify>
  <done>The "No Legacy" deletion of Phase 10 is officially justified.</done>
</task>
</tasks>
