---
phase: 05-test-coverage-quality
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - "apps/backend/tests/ai/test_assistant.py"
  - "apps/backend/tests/ai/test_report_generator.py"
  - "apps/backend/tests/ai/test_llm.py"
  - "apps/backend/tests/processing/test_enhancers.py"
autonomous: true
requirements: ["COV-02"]
must_haves:
  truths:
    - "Generative AI and Computer Vision AI layers achieve >90% coverage."
    - "Expensive operations like LLM inferences or PyTorch/ONNX inferences are fully mocked."
  artifacts:
    - path: "apps/backend/tests/ai/test_report_generator.py"
      provides: "Tests for forensic report generator"
    - path: "apps/backend/tests/ai/test_llm.py"
      provides: "Tests for LLM integrations"
  key_links:
    - from: "apps/backend/tests/ai/"
      to: "apps/backend/src/ai/"
      via: "pytest mocks and imports"
---

<objective>
Write isolated tests for the AI layer (`src/ai/`) and Vision Processing layer (`src/processing/`).
Purpose: Ensure >90% test coverage without running heavy Deep Learning models or hitting paid GenAI APIs.
Output: Fast running, comprehensive test suites for `assistant.py`, `llm.py`, `report_generator.py` and processing pipelines.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ksante/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/backend/src/ai/
@apps/backend/src/processing/
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: GenAI Modules Coverage</name>
  <files>apps/backend/tests/ai/test_assistant.py, apps/backend/tests/ai/test_report_generator.py, apps/backend/tests/ai/test_llm.py</files>
  <behavior>
    - Test 1: Assistant properly routes queries without triggering actual LLM API calls.
    - Test 2: Report generator stitches context elements correctly and formats outputs.
  </behavior>
  <action>Create or update tests for `src/ai/assistant.py`, `src/ai/report_generator.py`, and `src/ai/llm.py`. Use `unittest.mock.patch` extensively to intercept LlamaIndex / OpenAI calls. Assert that the correct prompt templates and kwargs are passed to the mocked endpoints.</action>
  <verify>
    <automated>cd apps/backend &amp;&amp; PYTHONPATH=. pytest tests/ai/test_assistant.py tests/ai/test_report_generator.py tests/ai/test_llm.py -v</automated>
  </verify>
  <done>GenAI modules reach >90% coverage while tests execute in milliseconds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AI Vision & Processing Modules Coverage</name>
  <files>apps/backend/tests/processing/test_enhancers.py, apps/backend/tests/test_ai_pipeline.py</files>
  <behavior>
    - Test 1: Enhancers pipeline logic acts accurately when image processing functions return deterministic mocked data.
    - Test 2: AI pipeline correctly cascades from segmentation to extraction.
  </behavior>
  <action>Review and update existing tests for `src/processing/` and `src/ai/` vision modules (like `segmentation.py`, `extraction.py`, `enhancement.py`). Ensure PyTorch and ONNX loads are bypassed using the global fixtures or local mocks. Verify error handling logic for model load failures.</action>
  <verify>
    <automated>cd apps/backend &amp;&amp; PYTHONPATH=. pytest tests/processing/ tests/test_ai_pipeline.py -v</automated>
  </verify>
  <done>Processing logic achieves >90% coverage safely mocked.</done>
</task>

<task type="auto">
  <name>Task 3: Verify AI/Processing Coverage Goal</name>
  <files>apps/backend/pyproject.toml</files>
  <action>Run a coverage report targeting `src.ai` and `src.processing` to ensure the threshold is met. Patch any missing branch logic uncovered by the report.</action>
  <verify>
    <automated>cd apps/backend &amp;&amp; PYTHONPATH=. pytest tests/ai/ tests/processing/ --cov=src.ai --cov=src.processing --cov-fail-under=90</automated>
  </verify>
  <done>Coverage explicitly hits the 90% target for AI and Processing modules.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Testing vs External APIs | Tests must never hit external LLM endpoints (OpenAI, Anthropic). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Denial of Service | AI Tests | mitigate | Ensure strict global mocking of network requests to prevent test billing or API rate limiting. |
</threat_model>

<verification>
`pytest` execution does not trigger CUDA memory allocation or outbound HTTP requests to LLM providers.
</verification>

<success_criteria>
`pytest --cov=src.ai --cov=src.processing` outputs a coverage of >90%.
</success_criteria>

<output>
Create `.planning/phases/05-test-coverage-quality/05-03-SUMMARY.md` when done
</output>
