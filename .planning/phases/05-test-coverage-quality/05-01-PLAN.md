---
phase: 05-test-coverage-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "apps/backend/pyproject.toml"
  - "apps/backend/tests/conftest.py"
autonomous: true
requirements: ["COV-01", "COV-04"]
must_haves:
  truths:
    - "Test execution uses mocked versions of database and GPU resources to stay fast."
    - "Pytest fails if coverage drops below 90%."
  artifacts:
    - path: "apps/backend/pyproject.toml"
      provides: "pytest-cov configurations"
      contains: "--cov-fail-under=90"
    - path: "apps/backend/tests/conftest.py"
      provides: "Global reusable test fixtures"
      contains: "get_db"
  key_links:
    - from: "apps/backend/pyproject.toml"
      to: "apps/backend/tests/"
      via: "pytest execution rules"
---

<objective>
Configure the coverage infrastructure and testing environment for the backend to ensure >90% code coverage can be met safely. 
Purpose: We need fast test execution without real dependencies (like heavy ML models or databases), ensuring stability.
Output: Configured `pyproject.toml` forcing 90% coverage and `conftest.py` with mock fixtures.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ksante/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/backend/pyproject.toml
@apps/backend/tests/conftest.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Configure Pytest and Coverage</name>
  <files>apps/backend/pyproject.toml</files>
  <action>Update `pyproject.toml` to enforce a 90% minimum coverage. Add `--cov-fail-under=90` to `addopts` in `[tool.pytest.ini_options]` or an equivalent rule in `[tool.coverage.report]`. Ensure `pytest-cov`, `pytest-asyncio`, and `httpx` are available in the testing dependencies. Note: they might already be present; verify and adjust as necessary.</action>
  <verify>
    <automated>grep -q "cov-fail-under=90" apps/backend/pyproject.toml</automated>
  </verify>
  <done>Pytest configuration forces failure if coverage drops below 90%.</done>
</task>

<task type="auto">
  <name>Task 2: Setup Core Mock Fixtures</name>
  <files>apps/backend/tests/conftest.py</files>
  <action>Refactor or create `conftest.py` to include robust and reusable global fixtures. Create a mocked database session overriding FastAPI's `get_db` dependency. Create global auto-use mock fixtures for Heavy AI Models (like LlamaIndex or ONNX inference calls) to prevent them from hitting disk/GPU during isolated tests. Use `unittest.mock` to patch expensive processing functions.</action>
  <verify>
    <automated>cd apps/backend &amp;&amp; pytest tests/conftest.py --collect-only</automated>
  </verify>
  <done>FastAPI dependencies are intercepted and tests run fast utilizing mocked infrastructure.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test vs. Prod Environment | Test configurations must not leak into or affect production configurations |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `conftest.py` | accept | Test data mocked locally, no production PII is loaded |
</threat_model>

<verification>
Ensure `cd apps/backend && PYTHONPATH=. pytest` initializes correctly with the new configurations and doesn't load GPU memory if isolated tests are executed.
</verification>

<success_criteria>
`pyproject.toml` forces a 90% coverage threshold and `conftest.py` successfully mocks dependencies.
</success_criteria>

<output>
Create `.planning/phases/05-test-coverage-quality/05-01-SUMMARY.md` when done
</output>
