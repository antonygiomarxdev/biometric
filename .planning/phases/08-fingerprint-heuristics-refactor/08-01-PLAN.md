---
phase: 08-fingerprint-heuristics-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/interfaces.py
  - src/processing/pre_hooks.py
  - tests/processing/test_pre_hooks.py
autonomous: true
requirements:
  - CA-03
must_haves:
  truths:
    - "System defines explicit interfaces for pipeline stages"
    - "Pre-processors can successfully mask noisy regions and binarize images"
  artifacts:
    - path: "src/core/interfaces.py"
      provides: "IPreProcessor, IPostProcessor, IFeatureExtractor"
    - path: "src/processing/pre_hooks.py"
      provides: "QualityMasker, BinarizationHook"
  key_links:
    - from: "src/processing/pre_hooks.py"
      to: "src/core/interfaces.py"
      via: "IPreProcessor implementation"
      pattern: "class .*\\(IPreProcessor\\):"
---

<objective>
Define strict pipeline abstractions and implement initial pre-processing hooks.

Purpose: Fix severe false positives by cleaning the fingerprint input image before the core extractor runs, using variance masking (QualityMasker) and binarization.
Output: Core interfaces and verified pre-processors.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define core pipeline interfaces</name>
  <files>src/core/interfaces.py</files>
  <action>Define new `IPreProcessor` (method: `process(image: np.ndarray) -> np.ndarray`) and `IPostProcessor` (method: `filter(points: List[Point], image: np.ndarray) -> List[Point]`) in `src/core/interfaces.py`. Update `IFeatureExtractor` to return raw candidate points instead of filtering them internally. Ensure all use abc.ABC and strict typing.</action>
  <verify>
    <automated>python -m pyright src/core/interfaces.py</automated>
  </verify>
  <done>Interfaces defined with strict types and pyright passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Pre-processors (QualityMasker & BinarizationHook)</name>
  <files>src/processing/pre_hooks.py, tests/processing/test_pre_hooks.py</files>
  <behavior>
    - Test 1: QualityMasker masks blocks with variance below threshold
    - Test 2: BinarizationHook correctly thresholds standard image
  </behavior>
  <action>Implement `QualityMasker` (calculates block variance to mask out scars/cuts/blank areas by setting them to 0 or a mask value) and `BinarizationHook` (applies thresholding) inside `src/processing/pre_hooks.py`. Both must implement `IPreProcessor`. Ensure strict typing.</action>
  <verify>
    <automated>pytest tests/processing/test_pre_hooks.py -v</automated>
  </verify>
  <done>Pre-processors correctly implement the interface and unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File -> CPU processing | Image loading and memory allocation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Denial of Service | QualityMasker | mitigate | Limit maximum image dimensions before processing block variance |
</threat_model>