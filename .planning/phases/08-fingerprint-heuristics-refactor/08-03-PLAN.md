---
phase: 08-fingerprint-heuristics-refactor
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - src/services/fingerprint_service.py
  - tests/services/test_fingerprint_service.py
autonomous: true
requirements:
  - CA-03
must_haves:
  truths:
    - "FingerprintService orchestrates the pre, core, and post processing chain"
    - "Service can be injected with custom hook lists via constructor"
  artifacts:
    - path: "src/services/fingerprint_service.py"
      provides: "Refactored FingerprintService"
    - path: "tests/services/test_fingerprint_service.py"
      provides: "Integration tests for the chain of responsibility"
  key_links:
    - from: "src/services/fingerprint_service.py"
      to: "src/core/interfaces.py"
      via: "Dependency Injection of IPreProcessor, IFeatureExtractor, IPostProcessor"
      pattern: "List\\[IPreProcessor\\]"
---

<objective>
Refactor FingerprintService to orchestrate the new modular pipeline.

Purpose: Wire up the Pre-processors, Core Extractors, and Post-processors into a Chain of Responsibility pattern inside the FingerprintService, eliminating the hardcoded monolithic logic.
Output: A clean, DI-friendly FingerprintService with system integration tests.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fingerprint-heuristics-refactor/08-01-PLAN.md
@.planning/phases/08-fingerprint-heuristics-refactor/08-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor FingerprintService</name>
  <files>src/services/fingerprint_service.py, tests/services/test_fingerprint_service.py</files>
  <behavior>
    - Test 1: Service correctly sequences Pre -> Core -> Post stages
    - Test 2: Service processes points through multiple extractors in parallel/sequence
    - Test 3: Service handles empty minutiae lists safely
  </behavior>
  <action>Refactor `FingerprintService`. Constructor must accept `List[IPreProcessor]`, `List[IFeatureExtractor]`, `List[IPostProcessor]`. In the processing method: apply pre-hooks sequentially, run core extractors, then apply post-hooks sequentially. Return the final normalized fingerprint data.</action>
  <verify>
    <automated>pytest tests/services/test_fingerprint_service.py -v</automated>
  </verify>
  <done>FingerprintService uses DI for the chain and all unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API Router -> FingerprintService | Passing raw image bytes to processing service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Information Disclosure | FingerprintService | mitigate | Ensure intermediate masks/skeletons are not leaked in error traces |
</threat_model>