---
phase: 08-fingerprint-heuristics-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/processing/extractor.py
  - src/processing/post_hooks.py
  - tests/processing/test_extractor.py
  - tests/processing/test_post_hooks.py
autonomous: true
requirements:
  - CA-03
must_haves:
  truths:
    - "SkeletonMinutiaeExtractor is dumbed down to only return raw points"
    - "HarrisCornerExtractor is available as a voting mechanism"
    - "Post-processors filter noise and fuse candidates correctly"
  artifacts:
    - path: "src/processing/extractor.py"
      provides: "SkeletonMinutiaeExtractor, HarrisCornerExtractor"
    - path: "src/processing/post_hooks.py"
      provides: "QualityFilter, SpurRemover, EnsembleFusionFilter"
  key_links:
    - from: "src/processing/post_hooks.py"
      to: "src/core/interfaces.py"
      via: "IPostProcessor implementation"
      pattern: "class .*\\(IPostProcessor\\):"
---

<objective>
Refactor core extractors and implement post-processing heuristics.

Purpose: Isolate the core extraction logic (Crossing Number, Harris) from the filtering rules, allowing the post-processors to handle dropping false-positive candidates like spurs or low-quality regions.
Output: Simplified extractors and verified post-processors.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fingerprint-heuristics-refactor/08-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Simplify SkeletonMinutiaeExtractor & Implement HarrisCornerExtractor</name>
  <files>src/processing/extractor.py, tests/processing/test_extractor.py</files>
  <behavior>
    - Test 1: SkeletonMinutiaeExtractor returns all points without filtering
    - Test 2: HarrisCornerExtractor detects corner points appropriately
  </behavior>
  <action>Refactor `SkeletonMinutiaeExtractor` to just run the Crossing Number algorithm and return raw minutiae without heuristics. Implement `HarrisCornerExtractor` as a second core extractor implementing `IFeatureExtractor`. Both return raw candidate points.</action>
  <verify>
    <automated>pytest tests/processing/test_extractor.py -v</automated>
  </verify>
  <done>Extractors are simplified and unit tests confirm they return raw unfiltered points.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Post-processors (Quality, Spur, Ensemble)</name>
  <files>src/processing/post_hooks.py, tests/processing/test_post_hooks.py</files>
  <behavior>
    - Test 1: QualityFilter drops points inside masked regions
    - Test 2: SpurRemover drops termination/bifurcation pairs within distance threshold
    - Test 3: EnsembleFusionFilter returns only points present in both lists
  </behavior>
  <action>Implement `QualityFilter` (drops points in masked areas), `SpurRemover` (drops Termination+Bifurcation pairs close to each other), and `EnsembleFusionFilter` (keeps only points detected by both core extractors). All must implement `IPostProcessor`.</action>
  <verify>
    <automated>pytest tests/processing/test_post_hooks.py -v</automated>
  </verify>
  <done>Post-processors correctly filter point lists and unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Extractor -> Post-processor | Passing point coordinate arrays |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Denial of Service | SpurRemover | mitigate | Limit max minutiae count before performing pairwise distance calculations to avoid O(N^2) CPU exhaustion |
</threat_model>