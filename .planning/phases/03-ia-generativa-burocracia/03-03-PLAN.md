---
phase: 03-ia-generativa-burocracia
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified: ["apps/backend/src/schemas/dictamen_schema.py", "apps/backend/src/ai/report_generator.py"]
autonomous: true
requirements: ["GENAI-03"]

must_haves:
  truths:
    - "Generated reports adhere perfectly to the legally required JSON structure."
    - "The generated tone is formal, objective, and devoid of conversational 'assistant' traits."
  artifacts:
    - path: "apps/backend/src/schemas/dictamen_schema.py"
      provides: "Legal report validation structures"
      contains: "class DictamenPericial(BaseModel)"
    - path: "apps/backend/src/ai/report_generator.py"
      provides: "Structured LLM generation logic"
      contains: "generate_dictamen"
  key_links:
    - from: "apps/backend/src/ai/report_generator.py"
      to: "apps/backend/src/ai/llm.py"
      via: "gets the LLM configured for report generation"
      pattern: "LLMFactory\\.create\\(\"report\"\\)"
---

<objective>
Automate the drafting of the "Dictamen Pericial" (Forensic Report) using a structured AI generation pipeline. The LLM must output valid JSON conforming strictly to a formal Pydantic schema, ensuring the generated text uses proper legal terminology and maintains the chain of custody formatting.

Purpose: Saves the forensic expert up to 50% of paperwork time while enforcing legal reporting standards.
Output: Pydantic schemas and the async report generation pipeline using `as_structured_llm`.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ksante/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ia-generativa-burocracia/03-AI-SPEC.md
@.planning/phases/03-ia-generativa-burocracia/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pydantic Schema Definition for Dictamen</name>
  <files>apps/backend/src/schemas/dictamen_schema.py, apps/backend/tests/schemas/test_dictamen.py</files>
  <behavior>
    - Test 1: Valid dictamen payload successfully parses into the Pydantic models.
    - Test 2: Field descriptions are properly set for LLM guidance.
  </behavior>
  <action>
    Create `apps/backend/src/schemas/dictamen_schema.py`. Define `Evidencia` and `DictamenPericial` Pydantic models explicitly as shown in `03-AI-SPEC.md`, Section 4b. Ensure all fields (`numero_caso`, `resumen_hechos`, `hallazgos`, `conclusion`, `nivel_confianza`) include detailed `Field(description="...")` attributes, as these act as prompt instructions for the LLM.
  </action>
  <verify>
    <automated>pytest apps/backend/tests/schemas/test_dictamen.py</automated>
  </verify>
  <done>Pydantic models are ready to be used as structured output templates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Asynchronous Report Generator with Structured Outputs</name>
  <files>apps/backend/src/ai/report_generator.py, apps/backend/tests/ai/test_report_generator.py</files>
  <behavior>
    - Test 1: generate_dictamen(case_data) processes input and returns a DictamenPericial instance.
    - Test 2: Retry logic successfully catches pydantic.ValidationError up to 3 times.
  </behavior>
  <action>
    Create `apps/backend/src/ai/report_generator.py`. Implement an async function `generate_dictamen(case_id: str, sql_results: str) -> DictamenPericial`. 
    1) Get the LLM via `LLMFactory.create("report")`.
    2) Wrap it using `sllm = llm.as_structured_llm(DictamenPericial)`.
    3) Create a prompt template instructing the model to act as a "Perito informático experto en la legislación de Nicaragua", using strict legal Spanish, avoiding conversational tone.
    4) Call `sllm.acomplete()` and return the structured object. 
    Implement a simple try/except loop to retry (up to 3 times) if a schema validation error occurs.
  </action>
  <verify>
    <automated>pytest apps/backend/tests/ai/test_report_generator.py</automated>
  </verify>
  <done>The pipeline safely generates typed, structured forensic reports from raw factual data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI -> Legal Output | Unconstrained LLM output could hallucinate facts or insert conversational text into official legal documents. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | `DictamenPericial` generation | mitigate | Force Pydantic validation via `as_structured_llm` to ensure structural integrity and typed metrics. |
| T-03-07 | Repudiation | Report Generator | mitigate | Prompt template explicitly commands the AI to include exact hash/ID values without truncation. |
| T-03-08 | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Ensure `generate_dictamen` correctly formats dummy data into a valid `DictamenPericial` object without conversational filler.
</verification>

<success_criteria>
`apps/backend/src/ai/report_generator.py` leverages LlamaIndex's `as_structured_llm` and handles async completion properly.
</success_criteria>

<output>
Create `.planning/phases/03-ia-generativa-burocracia/03-03-SUMMARY.md` when done
</output>