---
phase: 03-ia-generativa-burocracia
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: ["apps/backend/src/db/readonly.py", "apps/backend/src/ai/assistant.py"]
autonomous: true
requirements: ["GENAI-02"]

must_haves:
  truths:
    - "System blocks write queries (INSERT, UPDATE, DELETE) during NLP queries by using read-only credentials/sessions."
    - "System translates natural language queries into valid SQL against the designated tables safely."
  artifacts:
    - path: "apps/backend/src/db/readonly.py"
      provides: "Read-only SQLAlchemy engine connection"
      contains: "create_engine"
    - path: "apps/backend/src/ai/assistant.py"
      provides: "LlamaIndex Text-to-SQL logic"
      contains: "NLSQLTableQueryEngine"
  key_links:
    - from: "apps/backend/src/ai/assistant.py"
      to: "apps/backend/src/db/readonly.py"
      via: "wraps the engine in LlamaIndex SQLDatabase"
      pattern: "SQLDatabase"
    - from: "apps/backend/src/ai/assistant.py"
      to: "apps/backend/src/ai/llm.py"
      via: "gets the LLM configured for SQL generation"
      pattern: "LLMFactory\\.create\\(\"sql\"\\)"
---

<objective>
Implement the Text-to-SQL engine for the NLP Assistant. This module translates natural language queries into SQL, executes them safely using a read-only database connection, and synthesizes a response.

Purpose: Enables the "Asistente Forense" to answer statistical and audit questions about cases and evidence using natural language.
Output: A read-only DB connector and an asynchronous NLP assistant module.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ksante/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ia-generativa-burocracia/03-AI-SPEC.md
@.planning/phases/03-ia-generativa-burocracia/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Read-Only Database Connection Setup</name>
  <files>apps/backend/src/db/readonly.py, apps/backend/tests/db/test_readonly.py</files>
  <behavior>
    - Test 1: get_readonly_engine() returns a SQLAlchemy engine configured for read-only access.
    - Test 2: The connection prevents state modifications (if using a specific DB role or connection arg).
  </behavior>
  <action>
    Create `apps/backend/src/db/readonly.py`. Configure a SQLAlchemy engine specifically intended for read-only query execution, ideally enforcing read-only transaction parameters (e.g., `execution_options={"isolation_level": "AUTOCOMMIT", "postgresql_readonly": True}` for psycopg). Expose a `get_readonly_engine()` function.
  </action>
  <verify>
    <automated>pytest apps/backend/tests/db/test_readonly.py</automated>
  </verify>
  <done>A secure, read-only database engine factory is available.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Text-to-SQL Assistant Query Engine</name>
  <files>apps/backend/src/ai/assistant.py, apps/backend/tests/ai/test_assistant.py</files>
  <behavior>
    - Test 1: get_assistant_query_engine() builds an NLSQLTableQueryEngine with the read-only DB and SQL LLM.
    - Test 2: The query engine specifies explicit tables to avoid OOM errors.
    - Test 3: ask_assistant(query) is asynchronous and returns a synthesized response.
  </behavior>
  <action>
    Create `apps/backend/src/ai/assistant.py`. Implement an asynchronous function `ask_assistant(query: str) -> str` that: 
    1) Fetches the LLM using `LLMFactory.create("sql")`.
    2) Initializes `SQLDatabase` with `get_readonly_engine()` and explicit `include_tables=["peritajes", "evidencia"]` (do NOT omit the table list).
    3) Creates an `NLSQLTableQueryEngine` and uses `.aquery(query)` to resolve the request.
    Mock the LlamaIndex components in tests to ensure wiring is correct.
  </action>
  <verify>
    <automated>pytest apps/backend/tests/ai/test_assistant.py</automated>
  </verify>
  <done>The async assistant safely processes natural language to SQL and returns a text response.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> AI -> Database | AI generates SQL based on user input, risking SQL Injection or DOS if executed with write privileges. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Elevation of Privilege | `NLSQLTableQueryEngine` | mitigate | Explicitly restrict the SQLAlchemy engine to read-only transaction options (`postgresql_readonly=True`). |
| T-03-04 | Denial of Service | `NLSQLTableQueryEngine` | mitigate | Constrain context window by explicitly declaring `tables=["peritajes", "evidencia"]` instead of entire schema. |
| T-03-05 | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Ensure `ask_assistant("¿Cuántos peritajes hay?")` successfully queries the DB and returns a synthesized response.
</verification>

<success_criteria>
`apps/backend/src/ai/assistant.py` is implemented using async LlamaIndex methods and strictly limits the database context.
</success_criteria>

<output>
Create `.planning/phases/03-ia-generativa-burocracia/03-02-SUMMARY.md` when done
</output>