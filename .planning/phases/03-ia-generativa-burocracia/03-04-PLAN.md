---
phase: 03-ia-generativa-burocracia
plan: 04
type: execute
wave: 3
depends_on: ["02", "03"]
files_modified: ["apps/backend/src/api/routers/genai.py", "apps/backend/src/main.py"]
autonomous: true
requirements: ["GENAI-04"]

must_haves:
  truths:
    - "Clients can query the NLP assistant via a REST endpoint."
    - "Clients can request the generation of a draft forensic report via a REST endpoint."
    - "API endpoints are non-blocking and do not freeze the FastAPI event loop during local LLM generation."
  artifacts:
    - path: "apps/backend/src/api/routers/genai.py"
      provides: "REST endpoints for GenAI features"
      contains: "router = APIRouter("
    - path: "apps/backend/src/main.py"
      provides: "Application entrypoint"
      contains: "app.include_router(genai.router)"
  key_links:
    - from: "apps/backend/src/api/routers/genai.py"
      to: "apps/backend/src/ai/assistant.py"
      via: "calls async ask_assistant"
      pattern: "await ask_assistant"
    - from: "apps/backend/src/api/routers/genai.py"
      to: "apps/backend/src/ai/report_generator.py"
      via: "calls async generate_dictamen"
      pattern: "await generate_dictamen"
---

<objective>
Expose the AI Assistant and Report Generator functionalities through asynchronous REST API endpoints in the FastAPI application.

Purpose: Connects the backend AI logic to the frontend, enabling forensic experts to use the GenAI features from the UI without blocking the server.
Output: A new `genai` router registered in the main FastAPI application.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ksante/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ia-generativa-burocracia/03-AI-SPEC.md
@.planning/phases/03-ia-generativa-burocracia/03-02-SUMMARY.md
@.planning/phases/03-ia-generativa-burocracia/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GenAI API Router</name>
  <files>apps/backend/src/api/routers/genai.py, apps/backend/tests/api/test_genai.py</files>
  <behavior>
    - Test 1: POST /api/v1/genai/assistant accepts a query and returns the synthesized response.
    - Test 2: POST /api/v1/genai/report/{caso_id} accepts a payload and returns a valid DictamenPericial JSON.
  </behavior>
  <action>
    Create `apps/backend/src/api/routers/genai.py`. Define a FastAPI `APIRouter` with prefix `/genai`. 
    Implement two asynchronous endpoints:
    1. `POST /assistant` - Accepts a simple Pydantic payload (e.g., `query: str`). Awaits `ask_assistant(query)` and returns the text.
    2. `POST /report/{caso_id}` - Accepts necessary context data payload. Awaits `generate_dictamen(caso_id, sql_results)` and returns the serialized `DictamenPericial`.
    Use dependency injection where appropriate. Mock the underlying AI modules for testing the router layer.
  </action>
  <verify>
    <automated>pytest apps/backend/tests/api/test_genai.py</automated>
  </verify>
  <done>Endpoints are defined, fully typed, asynchronous, and properly handle errors.</done>
</task>

<task type="auto">
  <name>Task 2: Register GenAI Router in Main Application</name>
  <files>apps/backend/src/main.py</files>
  <action>
    Import the new `genai` router in `apps/backend/src/main.py` (or the equivalent router aggregator file) and include it in the main FastAPI app instance (e.g., `app.include_router(genai.router, prefix="/api/v1", tags=["genai"])`).
  </action>
  <verify>
    <automated>pytest apps/backend/tests/api/test_genai.py</automated>
  </verify>
  <done>The GenAI endpoints are discoverable via the FastAPI Swagger UI and accessible at the expected paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External Client -> API | Client inputs natural language queries that will be processed by the LLM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Denial of Service | `/genai/report` | accept | Local GPU constraints mean concurrent requests will queue. Accepting risk for MVP, but endpoints MUST be async. |
| T-03-10 | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Ensure a `curl` POST to `/api/v1/genai/assistant` triggers the mock (or real) engine and returns a 200 OK.
</verification>

<success_criteria>
`apps/backend/src/api/routers/genai.py` exists and follows Clean Architecture principles.
</success_criteria>

<output>
Create `.planning/phases/03-ia-generativa-burocracia/03-04-SUMMARY.md` when done
</output>