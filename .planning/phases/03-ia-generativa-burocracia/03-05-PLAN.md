---
phase: 03-ia-generativa-burocracia
plan: 05
type: execute
wave: 4
depends_on: ["04"]
files_modified: ["apps/backend/src/ai/tracing.py", "apps/backend/src/main.py", "apps/backend/promptfooconfig.yaml"]
autonomous: true
requirements: ["GENAI-05"]

must_haves:
  truths:
    - "LlamaIndex execution paths, token usage, and latency are traced via OpenTelemetry during local execution."
    - "Automated evaluation configurations exist for regression testing of prompts and schema compliance."
  artifacts:
    - path: "apps/backend/src/ai/tracing.py"
      provides: "OpenTelemetry and Arize Phoenix setup"
      contains: "LlamaIndexInstrumentor().instrument()"
    - path: "apps/backend/promptfooconfig.yaml"
      provides: "CI/CD Evaluation configuration"
      contains: "prompts:"
  key_links:
    - from: "apps/backend/src/main.py"
      to: "apps/backend/src/ai/tracing.py"
      via: "initializes tracing on application startup"
      pattern: "setup_tracing"
---

<objective>
Instrument the AI pipeline with observability tools and set up the automated evaluation framework.

Purpose: Provides visibility into local LLM execution (latency, token saturation, exact prompt sent) using Arize Phoenix and ensures future prompt changes can be regression-tested via Promptfoo/RAGAS.
Output: Tracing setup integrated into FastAPI lifespan and an evaluation configuration file.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ksante/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ia-generativa-burocracia/03-AI-SPEC.md
@.planning/phases/03-ia-generativa-burocracia/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup OpenTelemetry & Phoenix Tracing</name>
  <files>apps/backend/src/ai/tracing.py, apps/backend/src/main.py</files>
  <action>
    Create `apps/backend/src/ai/tracing.py`. Implement a `setup_tracing()` function that initializes `opentelemetry` TracerProvider and calls `LlamaIndexInstrumentor().instrument()`. Ensure it reads a config flag (e.g., `enable_ai_tracing`) to allow disabling it in purely production environments without a Phoenix server.
    Call `setup_tracing()` in `apps/backend/src/main.py` within the FastAPI `lifespan` context manager or startup event.
  </action>
  <verify>
    <automated>grep "setup_tracing" apps/backend/src/main.py</automated>
  </verify>
  <done>Tracing is conditionally enabled on application startup.</done>
</task>

<task type="auto">
  <name>Task 2: Setup Promptfoo Eval Configuration</name>
  <files>apps/backend/promptfooconfig.yaml</files>
  <action>
    Create `apps/backend/promptfooconfig.yaml`. Define a basic evaluation suite targeting the LLM prompts used in the `report_generator.py`. Set up test cases verifying schema adherence (is-json) and checking for absence of conversational language.
  </action>
  <verify>
    <automated>grep "prompts:" apps/backend/promptfooconfig.yaml</automated>
  </verify>
  <done>A base configuration file for CI/CD prompt regression testing exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application -> Tracing Server | Sensitive case data sent to telemetry server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Information Disclosure | `LlamaIndexInstrumentor` | mitigate | Tracing must only be enabled pointing to local/on-premise Phoenix instances. No external SaaS telemetry. |
| T-03-12 | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Ensure FastAPI starts up successfully when `enable_ai_tracing` is both True and False.
</verification>

<success_criteria>
Observability and evaluation tools are wired up following the `03-AI-SPEC.md` specification.
</success_criteria>

<output>
Create `.planning/phases/03-ia-generativa-burocracia/03-05-SUMMARY.md` when done
</output>