---
phase: 03-ia-generativa-burocracia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["apps/backend/src/core/config.py", "apps/backend/src/ai/llm.py"]
autonomous: true
requirements: ["GENAI-01"]

must_haves:
  truths:
    - "System supports both local (Ollama) and remote (OpenAI) LLM generation via configuration."
    - "System correctly instantiates an LLM configured for specific use cases (sql vs report)."
  artifacts:
    - path: "apps/backend/src/ai/llm.py"
      provides: "LLM Factory and Adapters"
      contains: "class LLMFactory"
    - path: "apps/backend/src/core/config.py"
      provides: "LLM configuration settings"
      contains: "llm_provider"
  key_links:
    - from: "apps/backend/src/ai/llm.py"
      to: "apps/backend/src/core/config.py"
      via: "imports settings to initialize the correct LLM adapter"
      pattern: "config\\.llm_provider"
---

<objective>
Establish the foundational infrastructure for Generative AI in the backend using the Adapter/Strategy pattern to allow seamless switching between local models (Ollama) and cloud APIs (OpenAI).

Purpose: Provides a unified, extensible interface for instantiating LLMs tailored to different generation profiles (Text-to-SQL vs. Text Generation) while abstracting the underlying provider.
Output: Extensible `LLMFactory` and updated core configuration.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ksante/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ia-generativa-burocracia/03-AI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Configuration for LLM</name>
  <files>apps/backend/src/core/config.py, apps/backend/tests/core/test_config.py</files>
  <behavior>
    - Test 1: Config model successfully parses LLM related environment variables (llm_provider="local", local_model_name="llama3.1:latest", etc.)
    - Test 2: Defaults are applied when variables are not present.
  </behavior>
  <action>
    Update the main Settings or Config class to include GenAI configuration parameters based on `03-AI-SPEC.md`. Add `llm_provider` (default "local"), `local_model_name` (default "llama3.1:latest"), `remote_model_name` (default "gpt-4"), and `openai_api_key`. Add relevant tests.
  </action>
  <verify>
    <automated>pytest apps/backend/tests/core/test_config.py</automated>
  </verify>
  <done>Configuration loads successfully with LLM parameters present and typed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement LLM Factory and Providers</name>
  <files>apps/backend/src/ai/llm.py, apps/backend/tests/ai/test_llm.py</files>
  <behavior>
    - Test 1: LLMFactory.create() returns an Ollama instance when config.llm_provider="local"
    - Test 2: LLMFactory.create() returns an OpenAI instance when config.llm_provider="openai"
    - Test 3: LLMFactory.create("sql") applies different timeout settings than default.
    - Test 4: ValueError raised on unknown provider.
  </behavior>
  <action>
    Implement `ILLMProvider` protocol, `OllamaProvider`, `OpenAIProvider`, and `LLMFactory` exactly as specified in `03-AI-SPEC.md`. Make sure the `get_llm(use_case="default")` handles parameter tuning (e.g., timeout=120.0 for "sql", 60.0 for others) and context_window constraints. Add basic tests mocking the returned LlamaIndex LLM instances.
  </action>
  <verify>
    <automated>pytest apps/backend/tests/ai/test_llm.py</automated>
  </verify>
  <done>LLMFactory correctly resolves and configures the LLM provider based on configuration and use case.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> Config | Configuration loading could inject malicious API keys if env is compromised |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `config.py` | mitigate | Ensure Pydantic `SecretStr` is used for `openai_api_key` to avoid accidental logging. |
| T-03-02 | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Ensure `LLMFactory.create("sql")` successfully returns a configured `LLM` object from LlamaIndex.
</verification>

<success_criteria>
`apps/backend/src/ai/llm.py` exists with `LLMFactory` following Clean Architecture and Strict Typing rules.
</success_criteria>

<output>
Create `.planning/phases/03-ia-generativa-burocracia/03-01-SUMMARY.md` when done
</output>