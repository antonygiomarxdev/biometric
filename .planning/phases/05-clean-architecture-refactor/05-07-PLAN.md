---
phase: 05-clean-architecture-refactor
plan: 07
type: execute
wave: 3
autonomous: true
---

<objective>
Escribir tests para fingerprint_service y pdf_generator, y refactorizar si es necesario.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests para fingerprint_service</name>
  <action>
    Crear `apps/backend/tests/services/test_fingerprint_service.py`.
    Mockear el enhancer y extractor (que son caros). Testear el flujo de orchestación
    (`process_image`, `process_batch`, `process_image_from_bytes`).
    >90% coverage.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests para pdf_generator</name>
  <action>
    Crear `apps/backend/tests/services/test_pdf_generator.py`.
    Testear la generación de PDF, la firma HMAC-SHA256, y el manejo de casos sin datos.
    Mockear WeasyPrint para no renderizar PDFs reales.
    >90% coverage.
  </action>
</task>

</tasks>
