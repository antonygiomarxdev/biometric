---
phase: 05-clean-architecture-refactor
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/services/matching_service.py
  - apps/backend/src/api/routers/known_fingerprints.py
autonomous: true
requirements: [CA-03]
must_haves:
  truths:
    - "Known fingerprints can be uploaded and processed exactly as before."
    - "The known fingerprints router does not contain DB transaction logic (no `db.commit()` or `db.add()`)."
    - "The router does not build or directly manipulate the `FingerprintVector` model."
  artifacts:
    - path: "apps/backend/src/services/matching_service.py"
      provides: "Extended `register_known` logic that handles DB persistence of `FingerprintVector`"
  key_links:
    - from: "apps/backend/src/api/routers/known_fingerprints.py"
      to: "apps/backend/src/services/matching_service.py"
      via: "Delegation of entire processing and persistence flow to the service"
---

<objective>
Refactor `known_fingerprints.py` to move the database insertion (`FingerprintVector`), vector mapping, and error handling logic into `MatchingService` (or a dedicated service if `MatchingService` becomes too bloated).

Purpose: Achieve total decoupling of HTTP concerns from business/DB concerns to facilitate testing and adhere to the Single Responsibility Principle.
Output: Refactored `MatchingService` managing the complete registration pipeline and an anemic router for `/known-fingerprints`.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ksante/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/backend/src/api/routers/known_fingerprints.py
@apps/backend/src/services/matching_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend MatchingService to Handle Persistence and Refactor Router</name>
  <files>apps/backend/src/services/matching_service.py, apps/backend/src/api/routers/known_fingerprints.py</files>
  <action>
    Update `MatchingService` (or a new `KnownFingerprintService` module if appropriate) to fully encapsulate the registration of known fingerprints. 
    Move the `FingerprintVector` instantiation, vector generation (`_build_query_vector`), and DB persistence (`db.add`, `db.commit`) from `known_fingerprints.py` into this service layer. 
    Refactor `known_fingerprints.py` to only handle the `UploadFile` extraction and delegate immediately to the service. The router MUST NOT import `src.db.models.FingerprintVector` and MUST NOT call `db.commit()` or `db.add()`.
  </action>
  <verify>
    <automated>grep -v '^#' apps/backend/src/api/routers/known_fingerprints.py | grep -c 'db\.commit' == 0</automated>
  </verify>
  <done>The `known_fingerprints` router is anemic and delegates entirely to the service. Persistence of `FingerprintVector` happens transparently within the service layer.</done>
</task>


<task type="auto">
  <name>Task 2: Update Tests for Matching Service</name>
  <action>
    Update existing tests or add new ones in `apps/backend/tests/services/test_matching_service.py` to cover the new logic (DB insertions for known fingerprints). Mock the DB session.
  </action>
  <acceptance_criteria>
    - Matching service tests pass and cover the new DB interaction.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service Layer | Encapsulates business logic, separating HTTP input from database execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | FingerprintVector Creation | mitigate | Creating DB models directly from untrusted input in the router is eliminated. Input is validated, processed, and persisted securely in the service layer. |
</threat_model>

<verification>
- Verify no `db.commit()` or `db.add()` exists in `apps/backend/src/api/routers/known_fingerprints.py`
- Verify the router no longer imports `src.db.models.FingerprintVector`.
</verification>

<success_criteria>
- The processing, vector generation, and persistence logic are completely moved to the service layer.
- The router acts strictly as an HTTP controller.
</success_criteria>

<output>
Create `.planning/phases/05-clean-architecture-refactor/05-03-SUMMARY.md` when done
</output>
