---
phase: 05-clean-architecture-refactor
plan: 05
type: execute
wave: 3
autonomous: true
---

<objective>
Crear CaseRepository, EvidenceRepository, DecisionRepository y refactorizar los services para usarlos.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repositorios para Cases, Evidence, Decision</name>
  <action>
    Crear en `apps/backend/src/db/repositories/`:
    - `case_repository.py`: CRUD completo para Cases (get, list, create, update, delete).
    - `evidence_repository.py`: CRUD completo para Evidence (get, list, create, delete, image_path lookup).
    - `decision_repository.py`: CRUD para Decisions (get, list, create).
    
    Cada repositorio encapsula TODO el SQLAlchemy (select, desc, add, commit, refresh, flush).
    Cada repositorio recibe `db: Session` por método.
  </action>
</task>

<task type="auto">
  <name>Task 2: Refactorizar Services</name>
  <action>
    Modificar `case_service.py`, `evidence_service.py`, `decision_service.py` para recibir los repositorios
    por inyección de dependencias en lugar de importar `from src.db.models import ...`.
    Los services ahora solo contienen lógica de negocio (validaciones, transformaciones, orchestación).
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests para repositorios y services actualizados</name>
  <action>
    Escribir tests unitarios para cada repositorio (mockeando Session).
    Actualizar tests de services para mockear los repositorios.
    >90% coverage en cada módulo.
  </action>
</task>

</tasks>
