---
phase: 05-clean-architecture-refactor
plan: 06
type: execute
wave: 3
autonomous: true
---

<objective>
Crear MatchingRepository y refactorizar MatchingService, más tests para auth_service.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MatchingRepository + Refactor</name>
  <action>
    Crear `apps/backend/src/db/repositories/matching_repository.py` con:
    - `insert_fingerprint_vector(session, data) -> FingerprintVector`
    Mover la lógica de INSERT del MatchingService al repositorio.
    MatchingService recibe el repo por DI.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests para auth_service</name>
  <action>
    Crear `apps/backend/tests/services/test_auth_service.py`.
    Testear: `verify_password`, `get_password_hash`, `create_access_token`, `decode_access_token`.
    Son funciones puras (no necesitan DB mockeada).
    >90% coverage.
  </action>
</task>

</tasks>
