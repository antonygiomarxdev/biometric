---
phase: 05-clean-architecture-refactor
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
  - 05-03
autonomous: true
files_modified:
  - "apps/backend/src/db/repositories/audit_repository.py"
  - "apps/backend/src/services/audit_service.py"
  - "apps/backend/tests/services/test_audit_service.py"
---

<objective>
Refactor Audit Service para cumplir Clean Architecture estricta.
Extraer el acceso a SQLAlchemy y el modelo `AuditLog` del servicio a un `AuditRepository`.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear AuditRepository</name>
  <action>
    Crear `apps/backend/src/db/repositories/audit_repository.py` con métodos:
    - `get_latest_entry(session) -> AuditLog | None` — busca el último log con FOR UPDATE.
    - `insert_entry(session, entry_data) -> AuditLog` — crea y hace flush de un nuevo AuditLog.
    - `lock_table(session)` — ejecuta `LOCK TABLE audit_log IN SHARE ROW EXCLUSIVE MODE`.
    El repositorio encapsula TODO el SQLAlchemy. El servicio no debe importar `AuditLog` ni `select()`.
  </action>
  <acceptance_criteria>
    - Repositorio tiene tests unitarios mockeando la sesión.
    - AuditService ya no importa `AuditLog` ni `sqlalchemy` (excepto `Session`).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactorizar AuditService + Tests</name>
  <action>
    Refactorizar `AuditService` para recibir `AuditRepository` por inyección de dependencias.
    El método `log_event` debe llamar a `repository.lock_table()`, `repository.get_latest_entry()`,
    `repository.insert_entry()` en lugar de hacer las queries directamente.
    Escribir tests unitarios completos (>90% coverage) mockeando el repositorio.
  </action>
  <acceptance_criteria>
    - AuditService no importa `AuditLog`, `select`, `desc`, `text`.
    - AuditService sigue produciendo el hash SHA-256 correcto.
    - Tests unitarios mockean el repositorio y verifican la cadena de hash.
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>
- AuditService cumple Clean Architecture (no importa ORM).
- AuditRepository encapsula todas las queries.
- >90% de cobertura en ambos módulos.
- SUMMARY.md creado.
</success_criteria>
