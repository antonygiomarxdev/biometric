{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn src.main:app --reload --host 0.0.0.0 --port 8000",
    "lint": "uv run ruff check src/",
    "typecheck": "uv run pyright src/",
    "test": "uv run pytest",
    "clean": "rm -rf src/*.egg-info .pytest_cache build dist"
  }
}
